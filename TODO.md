# 个人网页项目 TODO

文档维护人：agent
最后更新时间：2026-09-22（第二十一批：**文案与交互精修** —— Hero/关于重写（定位统一「步兵电控组负责人」）+ 技术栈 40→35 项及 3 条改名/升档 + 视频弹层方案 A + G1-D 双卡成果区改「纯结论」+ 004 正文补主张 + 实习区精简 + 文档全量巡检（修正 3 处失效脚本引用）+ 荣誉区 +1 条目；页高基线 → **15566/15522/15566**。前批见 git 历史，细则见 PROGRESS 快照与归档 INDEX）
范围：项目级 TODO，汇总 **v3-full（完整版）**未决事项与已知风险；会话过程记录见 PROGRESS.md。
> 分支定位：`v2` = 基础版（v2.4 线上状态，已冻结）；`v3-full` = 完整版（2026-09-12 由 `v3-rig-style` 改名），**后续改动均在 v3-full**。

## 优先级定义

| 优先级 | 含义 | 处理原则 |
| --- | --- | --- |
| P0 | 阻塞交付 / 严重 bug | 主流程跑不通或数据/安全问题，必须优先处理 |
| P1 | 核心功能缺口 | 不阻塞单机调试，但影响交付或联调 |
| P2 | 功能完善 / 正确性增强 | 主流程可跑，体验或状态准确性不足 |
| P3 | 技术债 / 工程化 | 不影响主流程，影响维护、部署与长期演进 |
| P4 | 远期规划 / 调研 | 无近期交付压力 |

## 状态标识

| 状态 | 含义 |
| --- | --- |
| `[ ]` | 待处理 |
| `[~]` | 进行中 / 已有部分实现 |
| `[x]` | 已完成（完成的条目定期清理，不累积历史） |
| `[!]` | 已知 bug / 风险（未解决前一直保留） |
| `[-]` | 暂缓 / 取消（写明原因，防止重走死路） |

## 待办（按优先级分组，组内按执行顺序排序）

### P1 功能缺口 / 无障碍命门

- [ ] **国内实测（承接域名上线，决定是否备案）**：手机流量（4G/5G 不挂代理）打开 `https://zhouzzw.online` 看首屏与视频；或用 itdog.cn / 17ce.com 全国测速 —— **满意即不备案**收工；慢则评估备案（¥80–160 + 1–3 周）。⚠️ 本机走代理，curl 数字不代表国内体验

- [ ] **v3 预览版仍在 GH Pages 入口，待回滚 v2 正式上线** —— 2026-09-21 把 `v3-full` `--no-ff` 合并到 `v2` 并推送（`d185ad1` run #3 → `8403a10` run #4，含 poster + 覆盖层修复）。GH Pages 现为 v3 预览版（**正式入口已是 CF + `zhouzzw.online`，本项只影响海外镜像**）。回滚：`git checkout v2 && git revert -m 1 8403a10 && git push origin v2`（软回滚，非破坏）；tag：`v2.4-frozen`(b00dae8) / `v3-preview`(d185ad1) / `v3-preview-2`(8403a10)

- [~] I4 视频弹窗自动播放策略（**2026-09-22 方案 A 已实现**）：覆盖层默认隐藏 —— 能自动播放则全程不遮挡 poster，仅「被拦截」亮 `点击播放` / 「缓冲」亮 `加载中 x%`（主动 `v.play()` 捕获拦截，取代 `autoplay` 属性）；**是否再加 `muted` 自动播仍待拍板**。I 节其余（I1–I9）全清

### P2 功能完善 / 正确性增强

- [-] 已否决 / 暂缓（勿重走死路；逐条理由见 PROGRESS 快照与归档 INDEX）：全站统一暗色 · 竖版视频裁切修复 · char-matrix 接入 · B6 锚点描边 · B7 Hero 刻度 · A4 简介区 12 栅格 · C3 旧配色与旧形态 · CTA 旧动效（生长 / 跳跃） · **CTA 箭头改 ↘ 方向（2026-09-21 提出后撤回）** · **卡片跟随高光（B5，2026-09-21 已移除 —— 判定与 img scale / 边框变色重复表达，勿再引入）** · **CloudBase 默认域名投放（2026-09-22 实测否决：腾讯云强制「测试域名」中间页 + 风控，官方定位仅测试用）** · **EdgeOne 大陆区免备案（官方文档明确：大陆加速必须 ICP 备案）** · **为「DNS 托管」而转注册商到 CF Registrar（无必要 —— 用「Connect a domain」即可；⚠️ 除非为**降低续费成本**，见运维手册决策备忘）**
- [ ] 全向轮步兵项目添加演示视频（用户 09-20 交付的 4 段均为 G1-D，无此项目素材；素材压 1080p30 CRF 26 → 入库 → 挂卡片）
- [~] 工业项目卡片（003 G1-D / 004 Isaac RL）素材补齐：**视频已挂 5 段**（003：大卡 Tesseract + 小卡 3 格[抓卡 demo/视觉伺服抓卡/鲁棒性]；004：视频占整卡位），两张第三方渲染图已停用（`g1d-arm-*`，**待定是否删除**）；实拍图仍缺
- [ ] 弹层终日子可读性（**待拍板**）：终端层占画面 37%（约 3.5px/字），小窗读不清 —— 可选 1440p 版本 / 调合成排版。（004 单段布局已于 09-21 按用户口径改为视频直接占整卡位，无占位图）
- [~] C3 档位与条目（**两轮校准完成，明细见 PROGRESS 快照**）：35 项定型；二轮 3 条改名 / 升档（`Gazebo / Rviz` · `DDS` · `sim2real` 域随机化）；档位分布 **12 熟练 / 22 熟悉 / 1 了解**（仅 MPC）
- [ ] A3 工具箱图标底衬（**需重新定方向**；「统一白底衬」已否决退回，勿再提）
- [ ] <768px 点「技术栈」导航后 kicker 被药丸遮挡（**待拍板**；顶部 48→≥72px，副作用移动端页高 +32px 需同步基线）
- [ ] A8 移动端筛选器滚动提示（**需先定形态**：右侧渐隐遮罩强度，露出下一项一角）
- [ ] 移动端螺旋展示（**待拍板**）：维持 <1024 隐藏（既定行为）· 或轻量版（只内 6–8 圈 + 慢转 + 按屏宽缩放，先出效果图）

### P3 技术债 / 工程化

- [ ] 项目区浏览成本（占全站 42.9%）→ 摘要栅格 + 详情弹窗（并入 C2：G1-D 时间轴 2026.03→2026.10，003/004 串「实习 → 落地」主线）
- [ ] 项目卡片缺显式「目标 / 问题」陈述（需用户提供每卡一句话目标，不代拟，补为 desc 上方标签块）
- [ ] 全局图片升级 `<picture>` + WebP + fallback
- [ ] ≤480px scrollspy 高亮被隐藏的「首页」项（回页顶指示器 0×0；跳 `display:none` 链接或改由「技术栈」承接 —— IA 取舍）
- [ ] 性能项：**仅剩 I13**（图片尺寸属性，预防性，CLS 实测 0）。**I14 字体自托管 / I15 字体 CSS 已随 09-22 外链清零一并解决**（Noto Sans SC 1082KB 远端分片 → 自托管子集 259KB，且不再阻塞首屏）；I12 已于 09-17 修复
- [ ] 跨浏览器验证（Safari / Firefox）：等宽字体渲染 · clamp 流式收敛 · `svh` 降级（需在用户机器补）
- [ ] I11 Hero 底部约 198px 空白 + `.hero__status-bar` 死样式（删死样式 + 调低移动端 `min-height`，或补底部元素）
- [ ] 螺旋残留负载 B/C 档（稳态 42.5 FPS 上限来自 16 圈旋转本身）—— B1 只转内 6–8 圈 · B2 精简不可见文本 · C1 分层合成 · C3 Canvas；验证走 `fps-profile.mjs`
- [ ] 未使用令牌 `--radius-full`（09-17 CTA 圆角 9999→8px 后失去引用）—— 删除令牌或重新使用二选一；当前差集唯一余额：**声明 62 / 引用 61**（工程红线 #2 要求差集为空）
- [~] 视频资源体积策略：8 段共 ~89MB。**已瘦身 1 段**（32.7→19.9MB，为过 25MiB 单文件上限）；其余 7 段是否统一转 CRF 28（可再压 ~20MB）**待拍板** —— CF 走海外节点，体积直接影响播放体验

### P4 远期规划 / 调研

- [ ] v3-full 完整版上线（`deploy.yml` 仅监听 `v2`；调 workflow 分支监听后推送即可，等用户决策）
- [ ] C1 能力数字面板（差异化尝试，无行业先例）：2 款竞赛机器人 / 8 大技术域 / 90.9% 到位率 / 4 项国奖，置于简介之后、技术栈之前
- [ ] C4 信息架构：导航 5 项降 4 项 —— 经历 / 荣誉合并为纵向时间线（需先确认 IA）
- [ ] 博客页（第 5 页，可选）

## Bug / 风险清单

| 优先级 | 状态 | 问题 | 影响 | 临时规避 |
| --- | --- | --- | --- | --- |
| P3 | [!] | git filter-repo 历史重写，旧 commit SHA 全部失效 | 引用旧 SHA 的文档/链接会找不到 | 以 `git log` 当前 SHA 为准 |
| P2 | [!] | **CF API Token 已在对话中暴露**（2026-09-22，待轮换） | 持有者可改写 / 删除 Pages 部署 | 轮换流程见 `docs/运维手册.md` 第七节（Delete → 新建 → 更新 `~/.cf_token` → 部署验证） |
