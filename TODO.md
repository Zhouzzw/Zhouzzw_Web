# 个人网页项目 TODO

文档维护人：agent
最后更新时间：2026-09-21（第十七批：**标签近距磁吸替代跟随高光** —— `.project-tag` 近距磁吸（R=140 / 8px）落地；B5 卡片跟随高光与 `--spot-x/--spot-y` 一并移除，令牌基线 62/61。第十六批：**播放键 hover 光圈画圆 + 暗区水印关键词** —— 封面 hover 改 SVG 画圆光圈（不再整圈变橙）；水印换成 ROS2 · LQR · VMC · ROBOTICS · RL · FREERTOS；CTA 箭头 ↘ 改动经用户撤回，维持 ↗。第十五批：G1-D 演示视频挂载收口 —— 003「大卡 + 小卡 3 格」（Tesseract / 视觉识别抓放卡 / 视觉伺服抓卡 / 鲁棒性），004 视频占整卡位；抓卡 demo 为竖版（封面按 16:9 裁切、弹层完整显示）；页高基准 **15698 / 15968 / 15986**。第十四批：四段视频转码入库（1080p30 CRF 26）。第十三批：可优化项全量对账（C1 数字面板 / C4 导航 IA / 跨浏览器验证 / 移动端螺旋 / 字体自托管）。前批见 git 历史，细则见 docs/review-v3.md）
范围：项目级 TODO，汇总 **v3-full（完整版）**未决事项与已知风险；会话过程记录见 PROGRESS.md。
> 分支定位：`v2` = 基础版（v2.4 线上状态，已冻结）；`v3-full` = 完整版（2026-09-12 由 `v3-rig-style` 改名），**后续改动均在 v3-full**。

## 优先级定义

| 优先级 | 含义 | 处理原则 |
| --- | --- | --- |
| P0 | 阻塞交付 / 严重 bug | 主流程跑不通或数据/安全问题，必须优先处理 |
| P1 | 核心功能缺口 | 不阻塞单机调试，但影响交付或联调 |
| P2 | 功能完善 / 正确性增强 | 主流程可跑，体验或状态准确性不足 |
| P3 | 技术债 / 工程化 | 不影响主流程，影响维护、部署与长期演进 |
| P4 | 远期规划 / 调研 | 无近期交付压力 |

## 状态标识

| 状态 | 含义 |
| --- | --- |
| `[ ]` | 待处理 |
| `[~]` | 进行中 / 已有部分实现 |
| `[x]` | 已完成（完成的条目定期清理，不累积历史） |
| `[!]` | 已知 bug / 风险（未解决前一直保留） |
| `[-]` | 暂缓 / 取消（写明原因，防止重走死路） |

## 待办（按优先级分组，组内按执行顺序排序）

### P1 功能缺口 / 无障碍命门

- [ ] I4 视频弹窗自动播放策略（**待拍板**）
  点封面后 `paused=true` 需再点一次 play —— 浏览器自动播放策略，代码无错；二选一：加 `muted` 自动播（演示视频多半要声音）/ 接受现状。I 节其余（I1–I9）全清

### P2 功能完善 / 正确性增强

- [-] 已否决 / 暂缓（勿重走死路；逐条理由见 review-v3「已否决」节与 PROGRESS 快照）：全站统一暗色 · 竖版视频裁切修复 · char-matrix 接入 · B6 锚点描边 · B7 Hero 刻度 · A4 简介区 12 栅格 · C3 旧配色与旧形态 · CTA 旧动效（生长 / 跳跃） · **CTA 箭头改 ↘ 方向（2026-09-21 提出后撤回）** · **卡片跟随高光（B5，2026-09-21 已移除 —— 判定与 img scale / 边框变色重复表达，勿再引入）**
- [ ] 全向轮步兵项目添加演示视频（用户 09-20 交付的 4 段均为 G1-D，无此项目素材；素材压 1080p30 CRF 26 → 入库 → 挂卡片）
- [~] 工业项目卡片（003 G1-D / 004 Isaac RL）素材补齐：**视频已挂 5 段**（003：大卡 Tesseract + 小卡 3 格[抓卡 demo/视觉伺服抓卡/鲁棒性]；004：视频占整卡位），两张第三方渲染图已停用（`g1d-arm-*`，**待定是否删除**）；实拍图仍缺
- [ ] 弹层终日子可读性（**待拍板**）：终端层占画面 37%（约 3.5px/字），小窗读不清 —— 可选 1440p 版本 / 调合成排版。（004 单段布局已于 09-21 按用户口径改为视频直接占整卡位，无占位图）
- [ ] C3 档位数据待确认（40 项占位；只改 `data-lv` 与档位文字，形态配色已定稿）
- [ ] C3 ROS 卡「DDS (CycloneDDS) · Gazebo」被省略号截断（**待拍板**：拆两条 / 加 `title`）
- [ ] A3 工具箱图标底衬（**需重新定方向**；「统一白底衬」已否决退回，勿再提）
- [ ] <768px 点「技术栈」导航后 kicker 被药丸遮挡（**待拍板**；顶部 48→≥72px，副作用移动端页高 +32px 需同步基线）
- [ ] A8 移动端筛选器滚动提示（**需先定形态**：右侧渐隐遮罩强度，露出下一项一角）
- [ ] 移动端螺旋展示（**待拍板**）：维持 <1024 隐藏（既定行为）· 或轻量版（只内 6–8 圈 + 慢转 + 按屏宽缩放，先出效果图）

### P3 技术债 / 工程化

- [ ] 项目区浏览成本（占全站 42.9%、单卡约 1570px）→ 摘要栅格 + 点击详情弹窗（与原 P4「卡片详情弹窗」合并）；并入 C2：G1-D 项目时间轴（2026.03→2026.10，003/004 串「实习 → 落地」主线）
- [ ] 项目卡片缺显式「目标 / 问题」陈述（需用户提供每卡一句话目标，不代拟，补为 desc 上方标签块）
- [ ] 全局图片升级 `<picture>` + WebP + fallback
- [ ] ≤480px scrollspy 高亮被隐藏的「首页」项（回页顶指示器 0×0；跳 `display:none` 链接或改由「技术栈」承接 —— IA 取舍）
- [ ] review-v3 性能项（I13 / I14 / I15；**I12 已于 09-17 修复**）
  I13 图片尺寸属性（预防性，CLS 实测 0）· I14 需正常外网复测（沙箱 jsdelivr 不可达，属环境非代码）· I15 字体 CSS 异步化；**Noto Sans SC 子集 1082KB** 为第二大项
- [ ] 字体自托管 + 子集化（I14 根治）—— 螺旋约 30 个唯一字符优先（woff2 子集仅几 KB，跨设备 100% 一致），Noto Sans SC 1082KB 随后
- [ ] 跨浏览器验证（Safari / Firefox）—— 唯一未完成验证维度：等宽字体渲染 · clamp 流式收敛 · `svh` 降级；需在用户机器补
- [ ] I11 Hero 底部约 198px 空白 + `.hero__status-bar` 死样式（删死样式 + 调低移动端 `min-height`，或补底部元素）
- [ ] 螺旋残留负载 B/C 档（稳态 42.5 FPS 上限来自 16 圈旋转本身）—— B1 只转内 6–8 圈 · B2 精简不可见文本 · C1 分层合成 · C3 Canvas；验证走 `fps-profile.mjs`
- [ ] 未使用令牌 `--radius-full`（09-17 CTA 圆角 9999→8px 后失去引用）—— 删除令牌或重新使用二选一；当前差集唯一余额：**声明 64 / 引用 63**（工程红线 #2 要求差集为空）
- [ ] 视频资源体积策略（**待拍板**）：现 7 段共 ~97MB、单段最大 32.7MB（原约定 ≤10MB）；可选 —— 统一转 CRF 28（预计合计 ~63MB）/ 按 SOP 裁 45–60s 精华版 / 维持现状（`preload="none"` + IO 门控，点击才拉流）

### P4 远期规划 / 调研

- [ ] v3-full 完整版上线（`deploy.yml` 仅监听 `v2`；调 workflow 分支监听后推送即可，等用户决策）
- [ ] C1 能力数字面板（差异化尝试，无行业先例）：2 款竞赛机器人 / 8 大技术域 / 90.9% 到位率 / 3 项国奖，置于简介之后、技术栈之前
- [ ] C4 信息架构：导航 5 项降 4 项 —— 经历 / 荣誉合并为纵向时间线（需先确认 IA）
- [ ] 博客页（第 5 页，可选）

## Bug / 风险清单

| 优先级 | 状态 | 问题 | 影响 | 临时规避 |
| --- | --- | --- | --- | --- |
| P3 | [!] | git filter-repo 历史重写，旧 commit SHA 全部失效 | 引用旧 SHA 的文档/链接会找不到 | 以 `git log` 当前 SHA 为准 |
