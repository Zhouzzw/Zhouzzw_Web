# 个人网页项目 TODO

文档维护人：agent
最后更新时间：2026-09-11（首次创建：汇总 v2 分支 PROGRESS.md 待完成项 + v3 分支 HANDOVER.md next_steps）
范围：项目级 TODO，汇总 v2/v3 双线未决事项、已知 bug/风险与验收重点。会话过程记录见 PROGRESS.md。

## 优先级定义

| 优先级 | 含义 | 处理原则 |
| --- | --- | --- |
| P0 | 阻塞交付 / 严重 bug | 主流程跑不通或数据/安全问题，必须优先处理 |
| P1 | 核心功能缺口 | 不阻塞单机调试，但影响交付或联调 |
| P2 | 功能完善 / 正确性增强 | 主流程可跑，体验或状态准确性不足 |
| P3 | 技术债 / 工程化 | 不影响主流程，影响维护、部署与长期演进 |
| P4 | 远期规划 / 调研 | 无近期交付压力 |

## 状态标识

| 状态 | 含义 |
| --- | --- |
| `[ ]` | 待处理 |
| `[~]` | 进行中 / 已有部分实现 |
| `[x]` | 已完成（完成的条目定期清理，不累积历史） |
| `[!]` | 已知 bug / 风险（未解决前一直保留） |
| `[-]` | 暂缓 / 取消（写明原因，防止重走死路） |

## 待办（按优先级分组，组内按执行顺序排序）

### P1 核心功能缺口

- [ ] v3 线部署上线
  影响：v3 rig 风格全站初版已完成、构建通过，但未部署，无法线上验收
  处理：先确认部署目标（GitHub Pages / Vercel 偏好未定，勿自行启动）→ vite.config.js 配 subpath base（参照 v2 的 `/Zhouzzw_Web/` 经验）→ 复用 deploy.yml 思路建 workflow → push origin/v3-rig-style 验证线上 200

### P2 功能完善 / 正确性增强

- [!] 「旋转跳跃演示」视频被 16:9 容器裁成横带（v2 线上）
  影响：竖版视频 2160×3840 + `object-fit: cover` → 只显示中间一条横带
  处理：该项目卡片换竖版容器（改 `aspect-ratio` 或 `object-fit: contain`）
- [ ] char-matrix.js 接入技术栈区（v2）
  影响：Canvas 字符矩阵已写好但未接入，技术栈区仍用静态水印
  处理：techstack section 背景引 char-matrix 替换 text-watermark，与现有分类筛选共存
- [ ] 全向轮步兵项目添加演示视频（v2）
  影响：项目卡片缺演示
  处理：素材压 1080p H.264 CRF 26 ≤10MB → 入库 → 挂卡片
- [ ] SEO meta 标签优化（v2）
  处理：补 title/description/OG/JSON-LD，构建后验证 200
- [ ] v3 图片压缩转 WebP（v3）
  影响：项目照片 3.8MB/5.7MB 过大
  处理：同 v2 压缩管线（长边 ≤1920 + `-q:v 3`），输出 WebP + JPG fallback
- [ ] v3 红色装饰横线增至 6-10 根（v3，spec 要求）
  影响：当前仅 2 根伪元素实现，未达 spec
  处理：多个绝对定位 div 或 JS 动态生成
- [ ] v3 移动端浏览器实测（v3）
  影响：仅 CSS 断点适配，未在真机/浏览器验证
  处理：桌面 + <768px 逐区过 7 大区块，记录问题
- [ ] v3 中文字体回退渲染确认（v3）
  影响：Space Grotesk 无中文字符，回退 Noto Sans SC 的协调性未经确认
  处理：浏览器对比中英文混排效果

### P3 技术债 / 工程化

- [~] v3 终端 ASCII art 替换为像素风 RIG logo（已有简单文字版）
  处理：自定义像素风文字替换现有文字
- [ ] v3 终端区 4 标签连接线（spec 要求）
  处理：标签到终端窗口的 SVG 细线（1px #2a2a2a）
- [~] v3 噪点纹理 3% 浏览器实测（已上 3%，待确认）
  处理：不同显示器确认 3% 是否合适，按需调 2%-5%
- [ ] 全局图片升级 `<picture>` + WebP + fallback（v2）
  处理：图片换 picture 标签 + WebP，保留 JPG fallback
- [ ] v2 分支 PROGRESS.md 迁移到 progress skill 快照格式
  影响：当前为旧式长文档（无快照索引），与 v3 分支双线格式不一致
  处理：按 progress skill 重构（索引 + ≤3 快照），旧内容归档 docs/archive/
- [ ] CLAUDE.md 记忆路径与当前环境不符
  影响：文档指向 `D:\...`（Windows），实际环境为 `/home/qskj-2/...`
  处理：改指实际记忆路径

### P4 远期规划 / 调研

- [ ] 项目卡片点击弹出详情弹窗（v2）
- [ ] 博客页（第 5 页，可选）
- [ ] v3 定型后的合并策略：合回 v2 或换主分支（双线并行是既定决策，暂不动）

## Bug / 风险清单

| 优先级 | 状态 | 问题 | 影响 | 临时规避 |
| --- | --- | --- | --- | --- |
| P3 | [!] | git filter-repo 历史重写，旧 commit SHA 全部失效 | 引用旧 SHA 的文档/链接会找不到 | 以 `git log` 当前 SHA 为准；PROGRESS.md「Git 提交历史」已标注失效警告 |
