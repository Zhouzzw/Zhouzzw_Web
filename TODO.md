# 个人网页项目 TODO

文档维护人：agent
最后更新时间：2026-09-15（新增已否决两条；A2 / I2 / I4-playsinline 已完成并从清单移除；review 余项按档位登记在下方，逐项处置与证据见 docs/review-v3.md）
范围：项目级 TODO，汇总 **v3-full（完整版）**未决事项与已知风险。v2 为基础版冻结分支，不在此列。会话过程记录见 PROGRESS.md。
> 分支定位：`v2` 与 `v3-full` 并列。`v2` = 基础版（v2.4 线上状态）；`v3-full` = 完整版（2026-09-12 由 `v3-rig-style` 改名并覆盖为当前全部成果），**后续改动均在 v3-full**。原 `feature/g1d-industry-and-resume` 已删除。

## 优先级定义

| 优先级 | 含义 | 处理原则 |
| --- | --- | --- |
| P0 | 阻塞交付 / 严重 bug | 主流程跑不通或数据/安全问题，必须优先处理 |
| P1 | 核心功能缺口 | 不阻塞单机调试，但影响交付或联调 |
| P2 | 功能完善 / 正确性增强 | 主流程可跑，体验或状态准确性不足 |
| P3 | 技术债 / 工程化 | 不影响主流程，影响维护、部署与长期演进 |
| P4 | 远期规划 / 调研 | 无近期交付压力 |

## 状态标识

| 状态 | 含义 |
| --- | --- |
| `[ ]` | 待处理 |
| `[~]` | 进行中 / 已有部分实现 |
| `[x]` | 已完成（完成的条目定期清理，不累积历史） |
| `[!]` | 已知 bug / 风险（未解决前一直保留） |
| `[-]` | 暂缓 / 取消（写明原因，防止重走死路） |

## 待办（按优先级分组，组内按执行顺序排序）

### P2 功能完善 / 正确性增强

- [-] 全站统一暗色背景（已否决，防重走）
  说明：用户 2026-09-12 提出统一为暗色以消除「浅色岛分属两个导航模块」的困惑。实测站点已 70% 暗色（projects/techstack/生活/联系/footer），浅色岛（简介/实习/荣誉）是仅存的色调对比，且 hero 为刻意的明暗分屏；该困惑已通过「荣誉并入实习竞赛」在导航层解决，无需改色
- [-] 「旋转跳跃演示」视频 16:9 容器裁切（已否决修复，防重走）
  说明：竖版视频 2160×3840 在 16/9 + `object-fit: cover` 下只显示中间横带；2026-09-11 曾做竖版容器适配并经用户预览，**用户决定退回三项并列原样**，保留裁切展示。除非用户反悔，勿再动
- [-] char-matrix.js 接入技术栈区（已否决，防重走）
  说明：2026-09-11 已实现并经用户预览（Canvas 字符矩阵替代静态水印），**用户决定退回静态 text-watermark**。char-matrix.js 与 `.char-matrix-bg` 样式保留为备用，除非用户反悔，勿再主动接入
- [-] 锚点落点描边动效（已否决，防重走）
  说明：2026-09-15 按 review B6 实现 —— 跳转后给区块标题加橙色描边并渐隐（`section:target .section__title` + `@keyframes targetFlash`），**用户看后要求删除**，代码与专用变量 `--target-ring` 已全部移除。落点反馈改由导航药丸的 scrollspy 高亮承担，勿再提议
- [-] Hero SCROLL 滚动刻度指示器（已否决，防重走）
  说明：原元素含 `heroScrollCue` 下滑动效；2026-09-15 曾改为可点击跳转并加径向衬底以解决被螺旋文字压住的问题，**用户仍要求删除整块**（标记 + 全部样式 + 关键帧已清）。删除后螺旋圆环恢复连续 —— 当初为它加的衬底会在环纹上留一块空洞。勿再恢复
- [ ] 全向轮步兵项目添加演示视频
  影响：项目卡片缺演示
  处理：素材压 1080p H.264 CRF 26 ≤10MB → 入库 → 挂卡片
- [ ] 工业项目卡片（003 G1-D / 004 Isaac RL）补齐素材
  影响：目前各仅一张展示图，且为第三方 Mujoco 通用 G1 渲染图兜底；无演示视频
  处理：用户提供实拍图 / 演示视频后替换兜底图并挂载视频段（复用既有 `.project-item__section` 视频网格）。素材规格：图片长边 ≤1920 压缩、视频 1080p H.264 ≤10MB
- [ ] review-v3 第 1 档存量缺陷（A1 / A3 / A8 / A9）
  说明：A1 机械臂运控负责人折行 · A3 工具箱图标底衬不统一 · A8 移动端筛选器无滚动提示 · A9 页脚 ASCII 品牌字不可读
  处理：A8 遮罩形态、A9「提升可读 vs 换可读徽标」需用户先定；执行顺序见 docs/review-v3.md「D 节」第 1 档，逐项处置见「A 节」。**A2（全站无滚动入场动效）已于 2026-09-15 完成，连同类项 B1 一并清掉**

### P3 技术债 / 工程化

- [ ] 项目区浏览成本偏高
  影响：项目区占全站 42.9%（6796px），单卡约 1570px，4 卡纵向铺开需长距离滚动
  处理：摘要栅格 ＋ 点击展开详情；与 P4「项目卡片点击弹出详情弹窗」为同一方向，可合并推进
- [ ] 项目卡片缺显式的「目标 / 问题」陈述
  影响：块顺序已统一为 desc → 成果 → tags → 图，量化结果已前置；但卡片仍无一句点明目标或待解问题，案例结构不完整
  处理：需用户提供每个项目的一句话目标（不代拟文案），补为 desc 上方的「目标」标签块
- [ ] 全局图片升级 `<picture>` + WebP + fallback
  处理：图片换 picture 标签 + WebP，保留 JPG fallback
- [ ] ≤480px 时 scrollspy 高亮的是被隐藏的「首页」项
  影响：360 / 480px 实测回到页顶时滑动指示器宽高为 0（滚到下一区块自动恢复）；起因是 A14 修复后「首页」在 ≤480px 被隐藏
  处理：让 `main.js` 的 scrollspy 跳过 `display:none` 的链接，或改由「技术栈」承接页顶高亮（属 IA 取舍，需产品决策）
- [ ] review-v3 断点族 A11 / A12 / A15
  影响：1023 → 1024px 标题字号 71.6 → 48px（−33%）反向跳变；768–1023px 正文列贴边 16px；360–375px Hero 标题孤字
  处理：三者同源，建议一次改完；需先定「768–1023 是否引入第三档 `--gutter-tablet` 与第二段字号公式」。见「D 节」第 5 档
- [ ] review-v3 布局重排 A4 / A6
  影响：简介区头重脚轻（65ch 靠左 + 满宽大图）；项目描述右侧约 40% 空
  处理：需先确认视觉方向。见「D 节」第 4 档
- [ ] review-v3 I 节无障碍余项（I1 / I4 · 自动播放策略 / I6 / I8 / I9）
  影响：视频封面不可键盘打开（弹层本身已可用）· 点封面后视频因自动播放策略不自动播 · 筛选无 `aria-pressed` · 页脚版权 3.01:1 与筛选按钮 4.30:1 临界不达标
  处理：I1 把 `.video-cover` 由 `<div>` 改为可聚焦按钮（弹层语义/焦点管理已于 2026-09-15 就绪）；I4 剩余一半需在 `muted` 与「接受二次点击」之间二选一。**I2（弹层无 dialog 语义与焦点管理）与 I4 的 `playsinline` 已于 2026-09-15 完成**。见「D 节」第 7 档
- [ ] review-v3 性能项（I12 / I13 / I14 / I15）
  影响：首屏即下载 4.48MB 视频（冷加载共 7337KB）· 20/20 图无 width/height · Geist 全族从未加载（mono 落系统字体）· 字体 CSS 阻塞 1159ms
  处理：I12 需先定「视口内截帧 vs 补 3 张静态封面」；I15 须在 `npm run build` 产物上复测。见「D 节」第 7 档

### P4 远期规划 / 调研

- [ ] v3-full 完整版上线方案
  影响：部署 workflow（`.github/workflows/deploy.yml`）目前仅监听 `v2`；v2 已定为冻结基础版不接收 v3，完整版暂无线上途径
  处理：如需上线 v3-full，调整 workflow 分支监听（或新建 v3-full 部署 job）后推送即可；等用户决策
- [ ] 项目卡片点击弹出详情弹窗
- [ ] 博客页（第 5 页，可选）

## Bug / 风险清单

| 优先级 | 状态 | 问题 | 影响 | 临时规避 |
| --- | --- | --- | --- | --- |
| P3 | [!] | git filter-repo 历史重写，旧 commit SHA 全部失效 | 引用旧 SHA 的文档/链接会找不到 | 以 `git log` 当前 SHA 为准；PROGRESS.md「Git 提交历史」已标注失效警告 |
