# 个人网页项目 TODO

文档维护人：agent
最后更新时间：2026-09-12（终端分隔条改造 + 导航/页脚锚点顺序修正已落地；P1/P2 可自主项已清空，剩余均为等素材或等用户决策）
范围：项目级 TODO，汇总 **v3-full（完整版）**未决事项与已知风险。v2 为基础版冻结分支，不在此列。会话过程记录见 PROGRESS.md。
> 分支定位：`v2` 与 `v3-full` 并列。`v2` = 基础版（v2.4 线上状态）；`v3-full` = 完整版（2026-09-12 由 `v3-rig-style` 改名并覆盖为当前全部成果），**后续改动均在 v3-full**。原 `feature/g1d-industry-and-resume` 已删除。

## 优先级定义

| 优先级 | 含义 | 处理原则 |
| --- | --- | --- |
| P0 | 阻塞交付 / 严重 bug | 主流程跑不通或数据/安全问题，必须优先处理 |
| P1 | 核心功能缺口 | 不阻塞单机调试，但影响交付或联调 |
| P2 | 功能完善 / 正确性增强 | 主流程可跑，体验或状态准确性不足 |
| P3 | 技术债 / 工程化 | 不影响主流程，影响维护、部署与长期演进 |
| P4 | 远期规划 / 调研 | 无近期交付压力 |

## 状态标识

| 状态 | 含义 |
| --- | --- |
| `[ ]` | 待处理 |
| `[~]` | 进行中 / 已有部分实现 |
| `[x]` | 已完成（完成的条目定期清理，不累积历史） |
| `[!]` | 已知 bug / 风险（未解决前一直保留） |
| `[-]` | 暂缓 / 取消（写明原因，防止重走死路） |

## 待办（按优先级分组，组内按执行顺序排序）

### P2 功能完善 / 正确性增强

- [-] 「旋转跳跃演示」视频 16:9 容器裁切（已否决修复，防重走）
  说明：竖版视频 2160×3840 在 16/9 + `object-fit: cover` 下只显示中间横带；2026-09-11 曾做竖版容器适配并经用户预览，**用户决定退回三项并列原样**，保留裁切展示。除非用户反悔，勿再动
- [-] char-matrix.js 接入技术栈区（已否决，防重走）
  说明：2026-09-11 已实现并经用户预览（Canvas 字符矩阵替代静态水印），**用户决定退回静态 text-watermark**。char-matrix.js 与 `.char-matrix-bg` 样式保留为备用，除非用户反悔，勿再主动接入
- [ ] 全向轮步兵项目添加演示视频
  影响：项目卡片缺演示
  处理：素材压 1080p H.264 CRF 26 ≤10MB → 入库 → 挂卡片
- [ ] 工业项目卡片（003 G1-D / 004 Isaac RL）补齐素材
  影响：目前各仅一张展示图，且为第三方 Mujoco 通用 G1 渲染图兜底；无演示视频
  处理：用户提供实拍图 / 演示视频后替换兜底图并挂载视频段（复用既有 `.project-item__section` 视频网格）。素材规格：图片长边 ≤1920 压缩、视频 1080p H.264 ≤10MB

### P3 技术债 / 工程化

- [ ] 全局图片升级 `<picture>` + WebP + fallback
  处理：图片换 picture 标签 + WebP，保留 JPG fallback

### P4 远期规划 / 调研

- [ ] v3-full 完整版上线方案
  影响：部署 workflow（`.github/workflows/deploy.yml`）目前仅监听 `v2`；v2 已定为冻结基础版不接收 v3，完整版暂无线上途径
  处理：如需上线 v3-full，调整 workflow 分支监听（或新建 v3-full 部署 job）后推送即可；等用户决策
- [ ] 项目卡片点击弹出详情弹窗
- [ ] 博客页（第 5 页，可选）

## Bug / 风险清单

| 优先级 | 状态 | 问题 | 影响 | 临时规避 |
| --- | --- | --- | --- | --- |
| P3 | [!] | git filter-repo 历史重写，旧 commit SHA 全部失效 | 引用旧 SHA 的文档/链接会找不到 | 以 `git log` 当前 SHA 为准；PROGRESS.md「Git 提交历史」已标注失效警告 |
