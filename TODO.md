# 个人网页项目 TODO

文档维护人：agent
最后更新时间：2026-09-16（第三批：**A1 / I1 / I6 / I8 / I9 已落地**，I 节无障碍项收口；**生产构建复测完成**，见 review-v3 **K 节**；**A3「统一白底衬」实施后被用户看后要求退回**，播放键改暗色玻璃圆，见 **K4**。第二批 J1–J8 + I10 已落地。**已否决**：B6 / B7 / **A3 统一白底衬方向**。**细则与证据见 docs/review-v3.md**）
范围：项目级 TODO，汇总 **v3-full（完整版）**未决事项与已知风险；会话过程记录见 PROGRESS.md。
> 分支定位：`v2` = 基础版（v2.4 线上状态，已冻结）；`v3-full` = 完整版（2026-09-12 由 `v3-rig-style` 改名），**后续改动均在 v3-full**。

## 优先级定义

| 优先级 | 含义 | 处理原则 |
| --- | --- | --- |
| P0 | 阻塞交付 / 严重 bug | 主流程跑不通或数据/安全问题，必须优先处理 |
| P1 | 核心功能缺口 | 不阻塞单机调试，但影响交付或联调 |
| P2 | 功能完善 / 正确性增强 | 主流程可跑，体验或状态准确性不足 |
| P3 | 技术债 / 工程化 | 不影响主流程，影响维护、部署与长期演进 |
| P4 | 远期规划 / 调研 | 无近期交付压力 |

## 状态标识

| 状态 | 含义 |
| --- | --- |
| `[ ]` | 待处理 |
| `[~]` | 进行中 / 已有部分实现 |
| `[x]` | 已完成（完成的条目定期清理，不累积历史） |
| `[!]` | 已知 bug / 风险（未解决前一直保留） |
| `[-]` | 暂缓 / 取消（写明原因，防止重走死路） |

## 待办（按优先级分组，组内按执行顺序排序）

### P1 功能缺口 / 无障碍命门

- [ ] review-v3 I4 · 视频弹窗自动播放策略（**待你拍板**）
  影响：点封面后视频 `paused=true, currentTime=0`，需再点一次 play —— 是浏览器自动播放策略拦截，**代码无错**
  处理：二选一 —— 加 `muted` 自动播（但看演示视频多半要声音），或接受「需再点一次 play」。**I1 / I2 / I3 / I5 / I6 / I8 / I9 已完成**，全站文本对比度按第三轮扫描结论已达 AA。见「I 节」I-1

### P2 功能完善 / 正确性增强

- [-] 全站统一暗色背景 —— 站点已 70% 暗色，浅色岛是仅存色调对比、hero 为刻意明暗分屏；09-12 的归属困惑已在导航层解决
- [-] 「旋转跳跃演示」竖版视频 16:9 裁切修复 —— 09-11 做过竖版适配，用户预览后退回并列原样、保留裁切
- [-] char-matrix.js 接入技术栈区 —— 09-11 已实现（Canvas 字符矩阵替静态水印），用户预览后退回 `text-watermark`；代码留作备用
- [-] 锚点落点描边动效（review B6）—— 09-15 实现后**用户要求删除**，代码与 `--target-ring` 已清；落点反馈改由 scrollspy 承担
- [-] Hero SCROLL 滚动刻度（review B7）—— 09-15 改为可点击后**用户仍要求删除整块**（含 `heroScrollCue` 关键帧）；删除后螺旋圆环恢复连续
- [ ] 全向轮步兵项目添加演示视频
  处理：素材压 1080p H.264 CRF 26 ≤10MB → 入库 → 挂卡片
- [ ] 工业项目卡片（003 G1-D / 004 Isaac RL）补齐素材
  影响：各仅一张第三方 Mujoco 通用 G1 渲染图兜底，无演示视频
  处理：用户提供实拍图 / 视频后替换兜底图并挂视频段（复用 `.project-item__section` 视频网格）。规格：图长边 ≤1920、视频 1080p H.264 ≤10MB
- [ ] review-v3 A3（**需重新定方向**）
  说明：工具箱 9 个图标底衬不统一（仅 GitHub / ROS2 有白底块）。**「9 个统一白底衬」方案已于 2026-09-16 实施后被用户看效果否决并退回**，代码已还原原状
  处理：**勿再提「统一白底衬」**。如仍需处理，先定新方向（如只调那 2 枚白底块的尺寸 / 圆角，或给裸 logo 加**暗色**衬而非白衬）。见 review-v3 K4 / 「A 节」A3 行
- [ ] review-v3 A8 / A9（**需你先定形态**）
  说明：A8 移动端筛选器无滚动提示（`overflow-x:auto` + nowrap，无渐隐遮罩）· A9 页脚 ASCII 品牌字不可读（桌面 6px / <480px 直接 `display:none`）
  处理：A8 先定渐隐遮罩强度（露出下一项一角）；A9 先定「提到 8–9px」还是「换成可读徽标」。**A1 已于 2026-09-16 完成，A2 已于 09-15 完成**。见「D 节」第 1 档 / 「A 节」
### P3 技术债 / 工程化

- [ ] 项目区浏览成本偏高
  影响：占全站 42.9%（6796px）、单卡约 1570px，纵向铺开需长距离滚动
  处理：摘要栅格 ＋ 点击展开详情；与 P4「卡片详情弹窗」同向，可合并
- [ ] 项目卡片缺显式的「目标 / 问题」陈述
  影响：块顺序已统一、量化结果已前置，但无一句点明目标或待解问题
  处理：需用户提供每卡一句话目标（不代拟），补为 desc 上方的「目标」标签块
- [ ] 全局图片升级 `<picture>` + WebP + fallback
- [ ] ≤480px 时 scrollspy 高亮的是被隐藏的「首页」项
  影响：360 / 480px 回到页顶时滑动指示器 0×0（滚到下一区块即恢复）；起因是 A14 修复后「首页」在 ≤480px 被隐藏
  处理：让 scrollspy 跳过 `display:none` 的链接，或改由「技术栈」承接页顶高亮（IA 取舍，需决策）
- [ ] review-v3 断点族 A11 / A12 / A15
  影响：1023→1024px 标题 71.6→48px（−33%）反向跳变；768–1023px 正文列贴边 16px；360–375px Hero 孤字
  处理：三者同源，建议一次改完；需先定「768–1023 是否引入第三档 `--gutter-tablet` 与第二段字号公式」。见「D 节」第 5 档
- [ ] review-v3 布局重排 A4 / A6
  影响：简介区头重脚轻（65ch 靠左 + 满宽大图）；项目描述右侧约 40% 空
  处理：需先确认视觉方向；**J1 / J2 令牌化已于 2026-09-16 完成，前置约束已解除，当前仅差视觉方向决策**。见「D 节」第 4 档
- [ ] review-v3 性能项（I12 / I13 / I14 / I15）—— **已在生产产物上复测，数据见 review-v3 K3**
  影响：交互前即下载 **≈4.2MB** 视频（3 条封面已缓冲 3.2–4.0s，**生产产物同样复现**）· 20/20 图无 `width`/`height` · **Noto Sans SC 的 19 个 woff2 子集共 1082KB**（新发现，仅次于视频的第二大项）· 字体 CSS 仍阻塞（生产 DCL 839ms / FCP 1004ms）
  处理：I12 先定「视口内截帧 vs 补 3 张静态封面」（收益最大）；I13 补 `width`/`height` 或 `aspect-ratio`（预防性，实测 CLS＝0）；**I14 需在正常外网复测**——本沙箱 `cdn.jsdelivr.net` 不可达，Geist 样式表与工具图标均取不到，**属环境问题非代码问题**；I15 可自托管或 `media="print" onload` 异步化。见「D 节」第 7 档
- [ ] review-v3 I11 · Hero 底部约 198px 空白 + `.hero__status-bar` 死样式
  影响：390px 实测 Hero 内容底 498 / Hero 底 760（−padding 64）→ **底部约 198px 空白**；起因是该元素已从 HTML 删除但 3 个文件仍留样式，且 <1024 时既无状态栏、`.hero__scroll-indicator` 又是 `display:none`，底部没有任何锚定元素
  处理：删死样式 + 调低移动端 `min-height`，或补一个底部元素。见「I 节」I-3

### P4 远期规划 / 调研

- [ ] v3-full 完整版上线方案
  影响：`deploy.yml` 仅监听 `v2`，完整版暂无线上途径
  处理：调整 workflow 分支监听（或新建 job）后推送即可；等用户决策
- [ ] 项目卡片点击弹出详情弹窗
- [ ] 博客页（第 5 页，可选）

## Bug / 风险清单

| 优先级 | 状态 | 问题 | 影响 | 临时规避 |
| --- | --- | --- | --- | --- |
| P3 | [!] | git filter-repo 历史重写，旧 commit SHA 全部失效 | 引用旧 SHA 的文档/链接会找不到 | 以 `git log` 当前 SHA 为准 |
