# PROGRESS.md — 个人网页项目进度

> progress 会话快照（Δ 增量）。静态基线见 CLAUDE.md；未决事项见 TODO.md；更早快照见 `docs/archive/`。

**当前**：v3-full 主线（v2 基础版已冻结）；**线上正式入口 = https://zhouzzw.online（Cloudflare Pages，2026-09-22 绑定自定义域名）**，GH Pages 为海外镜像

## 📋 快照索引

| 日期 | 主题 | 摘要 | 状态 |
|------|------|------|------|
| 09-22 | 文案与交互精修（下）：项目卡润色 + 实习区精简 | G1-D 卡改叙事化（场景→链路→瓶颈→效果；成果 3 条含抓卡率 90%）；技术栈再校 3 条；页高 15634 → 15452 三档 | ✅ 完成 |
| 09-22 | 文案与交互精修（上）：Hero/关于重写 + 技术栈校准 + 视频弹层方案 A | 定位全站改「步兵电控组负责人」（5 处）；技术栈 40→35 项；弹层覆盖层默认隐藏；新增 `text-nowrap` / `br-desktop-only` | ✅ 完成 |
| 09-22 | 腾讯云痕迹清理 + 运维手册 | `dist-cb` → `dist-cf` 全面改名（3 处配置）+ README/CLAUDE 删 CloudBase 章节（部署目标 3→2、硬约束 5→4）+ 新增 `docs/运维手册.md` | ✅ 完成 |
| 09-22 | CTA 箭头字形修正（字体自托管连带影响） | Geist 的 `↗` 是方头粗短造型 → 箭头显式改用自托管 Noto Sans SC + 去描边 + 24px 补偿；三态与页高三档零回归 | ✅ 完成 |
| 09-22 | 自有域名上线：zhouzzw.online 全链路打通 | 注册局 NS → CF zone → Pages 双域名 HTTPS 200；顺带修 7 处硬编码 github.io、og:image 404、部署脚本缺 `--branch=main` | ✅ 完成 |

> 检索归档：`grep -n "#tag: <关键词>" docs/archive/*.md`。09-12「项目卡片分隔线」只提交未写快照（git `72254f6`）。

## 🏷️ 2026-09-22 · 文案与交互精修（下）：项目卡润色 + 实习区精简

**结论**：按用户逐条给出的新文案完成**项目区与实习区**一轮精修：技术栈再校 3 条档位，项目区导语精简并锁死断点；RoboMaster 卡修正 2 处口径；**G1-D 卡从"技术清单"改写为「场景 → 链路 → 瓶颈 → 效果」叙事**并重排为纯结论 3 条（精度 → 规划 → 响应）；004 卡同步改「直接结论」风格 + 正文补主张与价值锚点；实习区删导语段；09-23 荣誉区 +1 条目。页高 **15634 / 15671 / 15689 → 15566 / 15522 / 15566**。

| 维度 | 状态 |
|------|------|
| 主线 | ✅ 文案精修第一批（技术栈 / 项目卡 / 实习区 / 导语）完成，可继续其余区块 |
| commit | 本次提交，见 git log |
| 遗留 | 剩余区块（001/002/004 卡、荣誉 / 联系区）待过；其余 7 段视频瘦身待拍板 |

### ✅ 完成（Δ 自上篇）

| 项 | 位置 | 说明 |
|----|------|------|
| 技术栈再校 3 条 | `index.html` | `rclcpp / rclpy`(熟练) → **`Gazebo / Rviz`**(熟悉)；`DDS (CycloneDDS) · Gazebo`(了解) → **`DDS`**(熟悉)；`sim2real 域随机化` 了解 → 熟悉。档位分布定型：**12 熟练 / 22 熟悉 / 1 了解**（仅 MPC） |
| 项目区导语精简 | 同上 | 删中段「四个项目贯穿同一条主线：…两条技术路线」，保留首尾 |
| 两处断行锁死 | 同上 | 技术栈与项目区导语各插 `<br />`（不再随窗口宽度合并） |
| RoboMaster 卡修正 | 同上 | 描述「平滑移动与精准定位」→「**高机动平滑陀螺移动与云台精确定位**」；高光指标对调为「**底盘响应速度 30%，云台定位精度 25%**」（`data-to` 动画不变） |
| G1-D 两卡重写（003 + 004） | 同上 | **003**：正文删「CRC32 / 35 关节锁定」注与「VHACD / 双臂异步」段 → 加「场景 + 瓶颈」前因；成果改 **3 条纯结论**（静态到位误差 5mm / 3° → 单点规划周期 1.75s → 0.21~0.60s（约 5×）→ 伺服控制稳定追踪，响应 < 0.5s）。**004**：正文补「用端到端强化学习替代传统运动规划」主张 +「无规划节点，链路更短」价值锚点 +「目标契约」句合并；成果同改纯结论（到位率 90.9%（≥ 90% 验收线）· 50Hz 端到端直驱真机，无规划节点 · 到位判定 位置 < 3cm 且姿态 < 5° 真机复验通过） |
| 实习区精简 | 同上 | 删导语段（角色 / 项目 / 主导 / 跨端 4 要素在时间线列表均有承载） |
| 荣誉区新增条目（09-23） | `index.html` | 「RoboMaster 超级对抗赛 (全国赛) · 国家三等奖」插在南部赛区之后（赛区 → 全国递进）；荣誉区现 7 条 / 国家级奖项 **4 项** |
| 文档全量巡检 | `README` `运维手册` `tools/` | 修正 **3 处失效脚本引用**（`shrink-video*.sh` 不存在 → 改可执行 ffmpeg 命令）；运维手册文件数 34→35 + 凭据轮换流程 + 部署自检；README 补 `og-cover.jpg`。⚠️ **CF Token 曾暴露于对话** → 已记 TODO 风险，待轮换 |

### 🧭 决策

| 决策 | 结论 | 沉淀 |
|------|------|------|
| 卡片叙事 | G1-D 定「场景 → 链路 → 瓶颈 → 效果」+ 成果 3 条递进（规划层 → 感知层 → 最终效果） | ✅ CLAUDE 文案规范 |
| 删减前置检查 | 删文案前先确认信息在成果区 / 标签 / 列表有独立承载 | ✅ CLAUDE 文案规范 |
| 数字动画行改写 | 只改叙述文字，`data-to` / `data-suffix` 不动；指标对调时数值随名走 | ✅ 本快照 |

### 🔴 坑（勿重踩）

| 现象 | 根因 | 解决 | tag |
|------|------|------|-----|
| 加 `<br />` 后桌面端「看不出变化」 | 该宽度下本来就在同位置折行 | 正常：`<br />` 的意义是**锁死断点**（更宽窗口不再合并），移动端才明显 | #tag: css |
| 源码换行处凭空多一个空格 | HTML 把换行 + 缩进折叠为一个空格 | 换行点只落在「本来就该有空格」的边界（英文 / 数字旁、标点后），**汉字之间不断行** | #tag: css |

### 💡 关键发现

- #tag: docs — 卡片「显得单薄」的根因不是字少，而是三缺：**缺前因（挑战）· 指标缺意义锚点 · 缺工程规模感**；补一句「针对 XX 瓶颈」即可让技术清单变成故事
- #tag: docs — 删正文前先查成果区 / 标签 / 列表是否独立承载 —— 本批 4 处删除全部"零信息损失"

### 🚀 下会话指令

> 承接文案精修：上 / 下两篇完成，可继续剩余区块

1. **继续文案精修**（用户逐条给「原句 → 新句」）：001 / 002 / 004 卡、荣誉区、联系区
2. 待拍板：7 段视频瘦身（~89MB）· v3 回滚 v2 · I4 是否维持方案 A · 弹层终日子可读性
3. 已否决（勿再提）：转注册商到 CF Registrar（除非为降低续费成本）· CloudBase 默认域名 · EdgeOne 大陆免备案 · B5 跟随高光 · CTA 箭头 ↘
4. 复现：`node /tmp/probe/probe.mjs`（页高三档）· `/tmp/progress_sync.py`（文档同步脚本，含自检）
5. 基线：页高 **15566 / 15522 / 15566** · 令牌 62/61（未动 CSS 令牌）· 技术栈 35 项

## 🏷️ 2026-09-22 · 文案与交互精修（上）：Hero / 关于重写 + 技术栈校准 + 视频弹层方案 A

**结论**：完成站点「叙事与定位」一轮重写 —— Hero 副标题与关于区改为「向下写底层驱动…向上做运动规划…」双向叙事；**定位口径全站统一为「RoboMaster 步兵电控组负责人」**（meta / og / twitter / JSON-LD / 关于区 5 处）；技术栈按用户逐项校准 **40 → 35 项**并重排档位；视频弹层改**方案 A**（覆盖层默认隐藏，仅拦截 / 缓冲时亮出）。

> ⚠️ 本批为上一会话完成、随本次一并提交（细节以 git diff 为准）。

| 维度 | 状态 |
|------|------|
| 主线 | ✅ Hero / 关于 / 技术栈 / 视频弹层（方案 A）已落地 |
| commit | 随本批提交，见 git log |
| 遗留 | 技术栈「余项待继续校准」→ 下篇又校准 3 条 |

### ✅ 完成（Δ 自腾讯云清理批）

| 项 | 位置 | 说明 |
|----|------|------|
| Hero 副标题重写 | `index.html` | 「向下写底层驱动与实时控制，向上做运动规划与具身智能 —— 让算法真正驱动物理世界」；配 `.text-nowrap`（防「具身智能 / 真正」拆词）+ `.br-desktop-only`（桌面断行、<1024 隐藏） |
| 关于区重写 | 同上 | 「曾任校 RoboMaster 战队步兵电控组负责人、武汉晴山科技…：在校主导…在实习期间全权负责宇树 G1-D 机械臂运控」；删「大二在读（荆楚卓越人才计划）」与「Class of 2028」 |
| 定位口径修正 | meta / og / twitter / JSON-LD / 关于区 | 「RoboMaster 电控组负责人」→「**步兵**电控组负责人」（5 处统一） |
| 技术栈校准（C3 第一批） | 同上 | 40 → **35 项**：删 MSP-M0 / 整车通信网络设计 / 算法仿真验证 / GPU 并行环境采样 / ONNX Runtime；`STM32 系列 (F4/G4)` → `STM32`；CAN / UART 重排；IIC / SPI / C++ / Python → 熟悉；`Keil/CubeIDE` → `Keil/CubeMX`；`CAN 分析仪` → `VOFA 上位机`；`逻辑分析仪/示波器` → `万用表/示波器`；分类「具身智能 / 强化学习」→「**强化学习**」（筛选按钮 + 标题 + 英文标签同步） |
| 排版工具类 | `style.css` | 新增 `.text-nowrap`（防中文词内断裂）与 `.br-desktop-only`（≥1024 断行 / 移动端隐藏） |
| 视频弹层方案 A | `main.js` | 覆盖层默认 `hidden`（能自动播放则全程不遮挡 poster）；主动 `v.play()` 取代 `autoplay` 属性以捕获拦截；仅「被拦截」亮按钮 / 「缓冲」亮「加载中 x%」 |

### 🧭 决策

| 决策 | 结论 | 沉淀 |
|------|------|------|
| Hero / 关于叙事 | 从"罗列经历"改为"**能力方向 + 定位**"（底层驱动 ↔ 运动规划双向） | ✅ CLAUDE 文案规范 |
| 换行控制 | 长句在桌面端用 `<br class="br-desktop-only">` 断行（移动端不硬断） | ✅ CLAUDE 文案规范 |
| 视频弹层 | **方案 A**：不遮挡优先（能播就不出现），拦截 / 缓冲才亮覆盖层 | ✅ TODO I4 |

### 💡 关键发现

- #tag: css — 中文排版两个高频坑的通用解：**词内不拆**（`.text-nowrap`）+ **桌面断行不伤移动端**（`.br-desktop-only` 媒体查询隐藏）
- #tag: docs — 「定位口径」散布在多处（meta / og / twitter / JSON-LD / 关于区 / 项目卡 role / Hero），改口径要**全站 grep 同义词**（电控负责人 / 电控组负责人）防漏

### 🚀 下会话指令

> 承接本批：定位与叙事已重写，文案精修继续（见下篇快照）

## 🏷️ 2026-09-22 · 腾讯云痕迹清理 + 运维手册入库

**结论**：把已否决的腾讯云 CloudBase 路线**从仓库里彻底移除** —— 构建产物目录 `dist-cb` → **`dist-cf`**（`package.json` / `wrangler.toml` / 部署脚本 / `.gitignore` 四处同步，旧目录已删、构建复验通过），README 与 CLAUDE 的 CloudBase 章节全清（部署目标 3 → **2 个**、硬约束 5 → **4 条**）；同时新增 **`docs/运维手册.md`**（九节：上线结构 / 两个入口 / 日常操作 / 费用 / 维护清单 / 故障排查 / 凭据位置 / 概念速查 / 决策备忘），并查清腾讯云**云端残留**（环境 + 静态托管 + PG 实例，体验版至 2027-03-21、**不自动续费**）。

| 维度 | 状态 |
|------|------|
| 主线 | ✅ 仓库不再含腾讯云部署痕迹；运维手册入库 |
| commit | 见 git log |
| 遗留 | 腾讯云云端环境待处置（立即销毁 or 等 2027-03-21 自然到期）—— 需用户决策 |

### ✅ 完成（Δ 自箭头修正批）

| 项 | 位置 | 说明 |
|----|------|------|
| 产物目录改名 | `package.json` `wrangler.toml` `tools/deploy-cloudflare.sh` `.gitignore` | `dist-cb` → `dist-cf`（cb = CloudBase 命名残留）；旧目录删除；`npm run build:cf` 复验产物正确 |
| README 清理 | `README.md` | 删 CloudBase 入口行 / 部署说明 / 资源表（共 22 行）；「三个入口」→「两个」；补运维手册指针 |
| CLAUDE 清理 | `CLAUDE.md` | 部署目标表删 CloudBase 行；硬约束删「CloudBase 默认域名不可投放」条并重编号（五 → 四） |
| 运维手册 | `docs/运维手册.md`（新） | 面向所有者：结构 → 操作 → 费用 → 排查（按现象查表）→ 凭据 → 概念 → 决策备忘 |
| 云端排查（只读） | 腾讯云环境 | 状态 `NORMAL`、体验版至 **2027-03-21 23:59**、`IsAutoRenew: false`；静态托管 online；附带 PG 实例 `pgdb-ji4jjx6j`（本站不用） |

### 🧭 决策

| 决策 | 结论 | 沉淀 |
|------|------|------|
| 腾讯云路线 | **从仓库彻底移除**（不再只是标注「不可投放」）；仅保留 TODO 一条「已否决」记录防重走弯路 | ✅ README / CLAUDE |
| 产物目录命名 | `dist-cb` → `dist-cf`（与唯一部署目标 Cloudflare 对齐） | ✅ 四处配置 + 文档 |
| 云端环境处置 | 待用户决策：立即销毁（不可逆）or 等 2027-03-21 自然到期（无自动续费、无持续扣费风险） | — 见下会话指令 |

### 💡 关键发现

- #tag: docs — 「废弃一条技术路线」的完整清理面 = **配置（产物目录 / 脚本）+ 文档（入口表 / 章节）+ 决策记录（已否决）** 三层；只删文档会留下 `dist-cb` 这类误导性命名，下次改构建时会踩
- #tag: docs — 运维手册的价值在「**故障 → 处理**」直接映射（按现象查表）+ 凭据位置表，而不是复述原理；两者齐备才能真做到 5 分钟定位

### 🚀 下会话指令

> 承接本批：仓库已干净，等云端环境处置决策

1. **待决策**：腾讯云环境是否立即销毁（不可逆：静态托管文件 + PG 实例一并消失）；不销毁也无持续费用（至 2027-03-21、不自动续费）
2. 提交本批改动（配置改名 + 文档清理 + 运维手册 + 归档）
3. 待拍板（承接前批）：v3 回滚 v2 · 其余 7 段视频瘦身（~89MB）· I4 `muted` 自动播
4. 已否决（勿再提）：CloudBase 作为部署目标 · **为 DNS 托管而**转注册商（⚠️ 为**续费成本**而转 CF Registrar 是正解，见运维手册） · 箭头依赖系统字体 · 每年换新域名薅首年优惠
5. 复现：`docs/运维手册.md`（运维唯一入口）· `npm run build:cf`（产物 → `dist-cf/`）
6. 基线：页高 **15634 / 15968 / 16011** · 令牌 62/61 · 部署目标 **2 个**（CF 正式 + GH 镜像）

## 🏷️ 2026-09-22 · CTA 箭头字形修正：自托管字体的连带影响

**结论**：用户反馈「CTA 箭头变粗短，要恢复之前修长的样子」→ 排查确认**根因不是 CSS 参数，而是字体**：09-22 字体自托管后 Geist 的 `↗`（U+2197）字形首次真正生效，而它是「方头、笔画粗、箭杆短」造型（此前页面一直 fallback 到系统字体的细长斜箭头）。修法 = 给箭头**显式指定自托管 Noto Sans SC** + 移除 `-webkit-text-stroke` 描边 + 字号 22→24 补偿 Noto 较小的光学尺寸（移动端 20→22）。

| 维度 | 状态 |
|------|------|
| 主线 | ✅ `v3-full`：CTA 箭头恢复细长斜箭头（**动效逻辑一字未动**） |
| commit | 见 git log |
| 遗留 | 观感已由用户看三态图拍板确认；其余待办见 TODO |

### ✅ 完成（Δ 自域名上线批）

| 项 | 位置 | 说明 |
|----|------|------|
| 箭头字形修正 | `assets/css/style.css` | `.cta-split__right` 加 `font-family: 'Noto Sans SC', sans-serif`、删 `-webkit-text-stroke: 0.5px`、`font-size: 22px → 24px` |
| 移动端同步 | `assets/css/mobile.css` | 20px → 22px（同口径补偿） |
| 验证 | `/tmp/probe/{arrow-compare.html,arrow-cta.mjs,cta.mjs}` | 11 字形 × 配置对比 → Noto 24/700 最优；三态截图：两态箭头同形同位置、动效读数 `(-40,40) → (-5.4,5.4) → (0,0)` 正常；页高三档 **15634 / 15968 / 16011** 零回归；令牌 62/61（仅 `--radius-full`） |
| 文档 | `CLAUDE.md` 交互设计 · CTA 条目 | 标注「箭头字形必须锁定 Noto Sans SC」+ 原因与改法（改字号前先跑 arrow-cta.mjs 对比） |

### 🔴 坑（勿重踩）

| 现象 | 根因 | 解决 | tag |
|------|------|------|-----|
| 箭头突然从「细长斜箭头」变成「粗短方头」 | **符号字形随字体变化**：自托管让 Geist 的 `↗` 首次真正生效，此前一直 fallback 到系统字体（09-17 定稿时看到的是系统字形） | 符号类元素（箭头 ↗ / 度量符等）**显式指定字体族**，不依赖继承；换字体后回归检查所有符号字符 | #tag: css |
| headless 下 hover 不触发（读数恒为初始位移） | `@media (hover:hover) and (pointer:fine)` 门控在 headless 默认 `(hover:none)` 下整段失效 | 探针启动加 `--blink-settings=primaryHoverType=2,availableHoverTypes=2,primaryPointerType=4,availablePointerTypes=4`（已写入 `cta.mjs`） | #tag: 视觉验证 |

### 💡 关键发现

- #tag: css — **换字体 = 换字形**：自托管字体全面生效后，必须回归检查**符号 / 图标类字符**的观感（它们此前可能是系统字体在渲染，而设计评审时的"好看"未必来自目标字体）
- #tag: design — 「修长 vs 臃肿」不只由字号决定：`font-family` 的字形设计 + `-webkit-text-stroke` 描边共同决定视觉重量（描边对细笔画符号的"增肥"效果尤其明显）

### 🚀 下会话指令

> 承接箭头修正：已确认，等与域名上线批一起提交

1. 提交（见 git status；建议 `feat` 域名元数据 / `fix` 箭头字形 / `docs` 文档 三个 commit，或按需合并）
2. 待拍板：v3 回滚 v2 · 其余 7 段视频瘦身（~89MB）· I4 `muted` 自动播
3. 已否决（勿再提）：**箭头回退到「依赖系统字体」方案**（跨设备不一致，违背自托管初衷）· CloudBase 默认域名 · 域名转注册商
4. 复现：`node /tmp/probe/arrow-cta.mjs`（字号/字重候选对比，注入式）· `cta.mjs`（三态）· `probe.mjs`（页高）
5. 基线：页高 **15634 / 15968 / 16011** · 令牌 62/61 · 箭头 = **Noto 24px/700**（移动端 22px）

## 🏷️ 2026-09-22 · 自有域名上线：zhouzzw.online 全链路打通

**结论**：`zhouzzw.online`（阿里云首年 ¥1）完成 **注册局 → Cloudflare → Pages** 全链路绑定 —— 实名通过当天改 NS，注册局 1–2h 内同步，zone 转 Active 后 **根域 + www 双入口 HTTPS 200**（证书 GTS，至 12-21）。顺带修掉 3 个「绑域名必然暴露」的问题：**7 处硬编码 `github.io` 元数据**、**og:image 指向 404 路径**（分享缩略图长期拉不到）、**部署脚本缺 `--branch=main`**（`v3-full` 分支部署只落预览环境）。

| 维度 | 状态 |
|------|------|
| 主线 | ✅ `https://zhouzzw.online` + `www` 双入口 200；生产已部署 |
| commit | 见 git log（元数据 + 分享图 + 脚本 + 文档） |
| 遗留 | 国内实测 → 定备案 · v3 回滚 v2 决策 · 其余 7 段视频瘦身 |

### ✅ 完成（Δ 自可达性攻坚批）

| 项 | 位置 | 说明 |
|----|------|------|
| NS 切换 + zone 接入 | 阿里云 / CF | 「DNS 修改」填 `april`/`yoxall.ns.cloudflare.com` → CF「**Connect a domain**」建 zone → Pages Custom domains 加根域 + www（自动 CNAME + 证书） |
| 终验（线上） | — | 双域名 200（title 正确）· HTTP→HTTPS 301 · 证书 GTS 至 12-21 · 28 条资源 200 · 8 段视频 **206 + video/mp4**（可拖拽） |
| 元数据切域名 | `index.html` `public/*` | **7 处** `github.io` → `zhouzzw.online`：canonical · og:url · og:image · twitter:image · JSON-LD · sitemap · robots |
| 分享图重建 | `public/og-cover.jpg` | 原 og:image 指向 `assets/images/比赛人物照.jpg`，产物里是 **hash 名** → 两个入口都 404；改 `public/` 稳定路径 + 1200×630（107KB） |
| 部署脚本修复 | `tools/deploy-cloudflare.sh` | 补 `--branch=main`（`v3-full` ≠ `main` 时只更新 `<分支>.pages.dev`） |
| 生产部署 | CF Pages | 增量 4 文件 / 2.15s；meta / sitemap / robots / og-cover 全部复验 |

### 🧭 决策

| 决策 | 结论 | 沉淀 |
|------|------|------|
| 域名接入方式 | CF「**Connect a domain**」做 DNS 托管（免费）；**不**用「Transfer a domain」转注册商（付费续一年，无必要） | ✅ CLAUDE 硬约束 #5 + TODO 已否决 |
| 分享图策略 | `og:image` 一律放 `public/`（现 `og-cover.jpg`，1200×630），**禁止引用构建产物 hash 路径** | ✅ CLAUDE 硬约束 #5 |
| 元数据一致性 | 换域名时 canonical / og / sitemap / robots / JSON-LD **7 处同步改**，正式域名即规范 URL | ✅ CLAUDE 硬约束 #5 |

### 🔴 坑（勿重踩）

| 现象 | 根因 | 解决 | tag |
|------|------|------|-----|
| 本机 dig / nslookup 一律返回 `198.18.0.x` | 代理 fake-IP 劫持 UDP/53 | DNS 走 **DoH** 并多源交叉 | #tag: 视觉验证 |
| 实名通过后仍 NXDOMAIN | 注册局数据未下发（控制台显示的 NS 仅是账户侧记录） | 等 1–2h（激活 → 吸收 NS 变更），DoH 观察 | #tag: 部署 |
| CF 提示 "Transfer DNS management" | zone 不在账户 / 处于 `Pending Nameserver Update` | 先 Connect a domain 建 zone → 等 Active → 再加 Custom domains | #tag: 部署 |
| 绑域名后返回 **522** | CF 边缘未把 hostname 路由到 Pages 项目（Verifying 过渡态） | 等 5–15 分钟转 Active；`pages.dev` 200 证项目正常 | #tag: 部署 |
| 探针报「资源全回退成 HTML（200 + 102KB）」 | ① 手拼 URL 带进 `find` 的 `./` 前缀（`/assets./css/…`）② 产物是 **hash 名**，dev 期路径线上不存在 | 用**线上 HTML 里原样引用的 URL** 逐条验证，别手拼 | #tag: 视觉验证 |

### 💡 关键发现

- #tag: 部署 — **og:image 不能用构建产物路径**（Vite hash → 必然 404）；元数据是「绑域名」的隐藏工作量：**站点能访问 ≠ 完整上线**，canonical / og / sitemap / robots 决定收录与分享卡片
- #tag: 部署 — CF 语义：`Connect a domain`（DNS 托管，免费，要的）vs `Transfer a domain`（注册商转移，付费，不要）；Pages 自定义域名必须 **zone 先 Active**

### 🚀 下会话指令

> 承接域名上线：链路已通，等国内实测结论

1. **P0 国内实测**：手机流量（不挂代理）打开 `https://zhouzzw.online` + itdog.cn 全国测速 → 满意则**不备案**收工；慢则评估备案（¥80–160 + 1–3 周）
2. 待拍板：v3 是否回滚 v2（`git checkout v2 && git revert -m 1 8403a10 && git push origin v2`）· 其余 7 段视频瘦身（~89MB）
3. 待办：文案改写 D1–D14 回填
4. 已否决（勿再提）：**转注册商到 Cloudflare Registrar（付费）** · CloudBase 默认域名 · EdgeOne 大陆免备案 · B5 跟随高光 · CTA 箭头 ↘
5. 复现：`/tmp/domain-verify.sh`（链路验证）· `/tmp/check-domain.sh`（NS 进度）· `~/.cf_token`（部署凭据，勿入库）
6. 基线：页高 **15634 / 15968 / 16011** · 令牌 62/61 · 双入口 `zhouzzw.online`（正式）+ `zhouzzw.github.io/Zhouzzw_Web/`（镜像）
