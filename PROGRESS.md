# PROGRESS.md — 个人网页项目进度

> progress 会话快照（Δ 增量）。静态基线见 CLAUDE.md；未决事项见 TODO.md；更早快照见 `docs/archive/`。

**当前**：v3-full 主线（v2 基础版已冻结；线上仍为 v2.4 https://zhouzzw.github.io/Zhouzzw_Web/）

## 📋 快照索引

| 日期 | 主题 | 摘要 | 状态 |
|------|------|------|------|
| 09-17 | CTA 画面切换动效与螺旋性能优化 | CTA 定稿：画面自左下沿 ↗ 推入（箭头随画面、位置零跳变）+ 8px 圆角；螺旋 A 档：删每帧滤镜/延迟启动/不可见暂停 → 加载 12→32 FPS、稳态 17→42.5 | ✅ 完成 |
| 09-16 | C3 掌握度定稿与 CTA 对角跃迁动效 | C3 定稿 F+暖米灰（三轮迭代）；页脚像素字 640px（A9 修复）；CTA 跃变动效（09-17 已重做为画面切换式） | ✅ 完成 |
| 09-16 | A4 退回原版与项目成果字号放大 | A4 12 栅格经用户看效果否决退回；成果字号提到与描述同级；页高 15120 / 14407 / 14633 | ✅ 完成 |
| 09-16 | 布局重排 A4/A6（A4 随后被否决退回） | 项目卡描述与成果并排落地；简介区 12 栅格方案经用户看效果否决并退回原版 | ✅ 完成 |
| 09-16 | 断点族落地（gutter 流式 + 标题连续化） | A11/A12/A15 + 768–1023 导航压标题一次修完；gutter 三档统一入口，1023↔1024 字号 48↔48 | ✅ 完成 |

> 检索归档：`grep -n "#tag: <关键词>" docs/archive/*.md`。09-12「项目卡片分隔线」只提交未写快照（git `72254f6`）。

## 🏷️ 2026-09-17 · CTA 画面切换动效与首页螺旋性能优化

**结论**：CTA 按钮按用户三轮反馈定稿为**画面切换式动效**（带箭头的新画面自按钮**左下角沿 ↗ 推入**、箭头随画面滑入、两态相对位置零跳变、退出沿 ↙ 退回）；首页螺旋动画完成 A 档性能优化，**加载期 12 → 32 FPS、稳态 17 → 42.5 FPS**，页高基线 15315 不变。

| 维度 | 状态 |
|------|------|
| 主线 | ✅ `v3-full`：CTA 定稿 + 螺旋 A 档（B/C 档未做） |
| commit | `feat`/`perf` + `docs` 两笔，见 git log |
| 遗留 | 螺旋 B/C 档（稳态 42.5 上限来自 16 圈旋转本身）· 线上仍为 v2.4 · C3 档位仍占位 |

### ✅ 完成（Δ 自 C3/CTA 批次）

| 项 | 位置 | 说明 |
|----|------|------|
| CTA 动效定稿 | `style.css` `mobile.css` | 录屏逐帧像素分析（13 帧追踪）→ 画面切换式：新画面（橙块+黑箭头）自左下沿 ↗ 推入（块 0.18s / 箭头 0.26s，超出画面被裁剪非淡入）；退出沿 ↙（箭头 0.16s 加速飞出、块 0.22s 延迟 0.05s）；两态箭头 bbox 逐像素一致（47×47 同中心）；圆角 9999→8px；文字 14px/600；箭头 22px/700 + 0.5px 描边 |
| 螺旋 A1 删滤镜 | `style.css` | `filter: drop-shadow(0 0 60px)` 删除 —— 停转后 A/B 像素 diff **0.000%**（视觉贡献为零）；稳态 17→44 FPS |
| 螺旋 A2 延迟启动 | `index.html` `style.css` | `html.spiral-on` 门控（load+600ms）；无 JS 时静态不转（零退化） |
| 螺旋 A3 不可见暂停 | `animations.js` `style.css` | 滚出视口 / 切后台 → `.is-paused` → `animation-play-state: paused` |
| 螺旋 A4 减动效 | `style.css` | `prefers-reduced-motion` 停转（原缺失） |

### 🧭 决策

| 决策 | 结论 | 沉淀 |
|------|------|------|
| CTA 动效语言 | 弃「底块生长 + 箭头跳跃」→ **画面切换**：新画面连带箭头整体位移、箭头恒居中；推入方向 = 左下沿 ↗（用户口径：顺箭头指示方向更新） | ✅ CLAUDE.md |
| 螺旋滤镜 | 先停转 A/B 像素 diff 再决定删/替代 —— 实测 0.000% → 直接删（无需 radial 替代） | ✅ CLAUDE.md 红线 #6 |
| 螺旋首屏策略 | 延迟启动（load+600ms）换加载期 +167%；「稍后开转」作为既定取舍 | ✅ CLAUDE.md 红线 #6 |

### 🔴 坑（勿重踩）

| 现象 | 根因 | 解决 | tag |
|------|------|------|-----|
| A/B 截图 diff 10.57% 误读为「滤镜有视觉贡献」 | 两次独立加载的旋转**相位不同**，diff 混入内容位移噪声 | 对比动画元素必须先停转/同相位再截图（停转后 diff 0.000%） | #tag: 视觉验证 |
| `fps-profile.mjs` 输出 `undefined` | `Runtime.evaluate` 表达式带顶层 `return` 未包裹 | 与探针约定一致包 async IIFE | #tag: 视觉验证 |

### 💡 关键发现

- #tag: 性能 — `drop-shadow` 挂在动画根上：静止几乎零成本、叠加动画即每帧重算 60px 模糊（17 FPS）。正交 A/B（停转留滤镜 / 去滤镜留动画）可一次分离「滤镜」与「动画」两个因素
- #tag: 性能 — 16 圈 `textPath`（约 3 万字形 + `lengthAdjust=spacingAndGlyphs`）旋转本身把上限压在 ~44 FPS；彻底解决需 B/C 档（少转几圈 / 文字转路径 / 分层合成 / Canvas）
- #tag: 视觉验证 — 加载期残余长帧（max ~840ms）来自字体（1082KB/20 子集）与图片解码，与螺旋无关

### 🚀 下会话指令

> 承接 C3/CTA 批次：CTA 已定稿；螺旋 A 档落地，B/C 档待做

1. 螺旋 B/C 档（如仍觉负载高）：B1 只转内 6–8 圈 · B2 精简不可见文本 · C1 分层合成 · C3 Canvas
2. 待拍板：**C3 档位数据**（40 项占位）· ROS 卡 DDS 条目截断 · <768 kicker 遮挡 · A3 / A8 形态 · I4 视频自动播放 · v3-full 上线
3. 已否决（勿再提）：A3 统一白底衬 · A4 简介区 12 栅格 · 通栏黑带 · 全站暗色 · 竖版视频 · char-matrix · B6 / B7 · CTA 旧动效（生长 / 跳跃）
4. 基线：页高 **15315 / 15113 / 15181** · 令牌 64/64 · 螺旋性能：加载 32 / 稳态 42.5 FPS
5. 复现：`fps-profile.mjs <preset>`（baseline/no-filter/no-spin/hide-visual/no-both）· `check-spiral.js`（启动/暂停/减动效三态）· node 需先 export PATH

## 🏷️ 2026-09-16 · C3 掌握度定稿与 CTA 对角跃迁动效

**结论**：C3 技能掌握度经三轮迭代定稿 **F 形态（双行小卡片）+ 暖米灰条色 `#C8C1B2`**（橙条与 E 五格刻度均被否）；页脚品牌字改为**橙色投影像素字 SVG 并收缩到 640px**（修掉 A9）；`.cta-split` 两枚 CTA 复刻参考录屏的**对角跃迁动效**（箭头恒 `↗`，底块自左下角铺满至右上）。

| 维度 | 状态 |
|------|------|
| 主线 | ✅ `v3-full`：C3 定稿 + A9 修复 + CTA 动效 |
| commit | `feat` + `docs`（本批一次提交），见 git log |
| 遗留 | C3 档位为占位待用户确认 · ROS 卡 DDS 长条目截断待拍板 · 线上仍为 v2.4 |

### ✅ 完成（Δ 自 A4 退回批）

| 项 | 位置 | 说明 |
|----|------|------|
| C3 技能掌握度 | `index.html` `desktop.css` | 40 项 = 双行小卡片（名称 / 条 + 档位文字），两列网格；`data-lv` 驱动条长（100/60/20%）；条色 `#C8C1B2`。迭代：F 橙条被否「花哨」→ E 五格刻度仍否 → 定位为**配色问题** → 柔化后 F 定稿 |
| 页脚品牌字 | `index.html` `style.css` | 6px 灰 ASCII 字符画 → **8×9 点阵 SVG 像素字**（橙渐变 + 右下暗橙投影，`max-width: 640px`），移动端不再隐藏 |
| CTA 对角跃迁动效 | `index.html` `style.css` | `.cta-split`（hero 查看项目 / footer 联系我）：箭头恒 `↗`；hover 底块 `scale(0→1)` origin 左下角铺满 + 箭头「左下淡出 → 换位 → 右上淡入」；按下沿 ↗ 飞出、自左下飞回 + 下压 |
| 页高基线 | — | **15315 / 15113 / 15181**（1440/390/360；相对旧基线 +195 / +706 / +548） |

### 🧭 决策

| 决策 | 结论 | 沉淀 |
|------|------|------|
| C3 形态与条色 | 三轮迭代定稿 **F + 暖米灰 `#C8C1B2`**（A/B/C/D 四方案用户选 B） | ✅ CLAUDE.md（含铁律：**橙只给交互与焦点，不做长期装饰**） |
| 像素字配色/尺寸 | 站点橙系（不用参考图蓝绿）+ 640px（原 1040 偏大） | ✅ CLAUDE.md |
| CTA 动效语言 | **对角跃迁**：底块自左下角铺满、箭头恒 ↗ 且左下↔右上跃变（非平移 / 非旋转）；首版「平移 + 旋转 45°」被否 | ✅ CLAUDE.md |

### 🔴 坑（勿重踩）

| 现象 | 根因 | 解决 | tag |
|------|------|------|-----|
| F 形态进度条全部不可见（宽 0） | `flex-direction: column` 的 li 被继承的 `align-items: center` 压成收缩宽 | 显式 `align-items: stretch`（已写进上线 CSS 注释） | #tag: css |
| 重设同名动画不重启，抓不到中间帧 | 同动画名在 hover 规则已播放，inline 重设同名不重播 | 先 `style.animation='none'` + 强制 reflow 再设 | #tag: 视觉验证 |

### 💡 关键发现

- #tag: design — 「F/E 谁更乱」的真因是**橙色用量**而非版式：强调色铺 40 项即「花哨」，柔化条色后 F 立刻安静 → **判断视觉问题先把「版式」与「色彩」两个变量分离**
- #tag: 视觉验证 — 用**慢放动画**（duration 改 3s + 定时取样计算样式）验证跃变动效，比逐帧截图可控（截图有 ~100ms 延迟，抓不准中间帧）
- #tag: docs — 同一形态来回两次（F→E→F）时快照须记录**完整往返理由**，否则后续误判「F 已否决」

### 🚀 下会话指令

> 承接 C3 定稿：实现全部落地，剩余两项待拍板 + 动效微调

1. 待拍板：**C3 档位数据**（40 项占位，给清单即换）· **ROS 卡 DDS 长条目截断**（拆两条 or title 兜底）
2. 可选微调：CTA 跃变幅度（±4px）· 底块生长 280ms · 箭头跃变 420ms
3. 未做：跨浏览器复核（Safari / Firefox）· A8 / C4 / C5 / I11–I15 等 review 项
4. 复现：探针 `/tmp/probe/`（`cdp.mjs --mouse` 真实 hover 必备；node 先 export PATH）
5. 基线：页高 **15315 / 15113 / 15181**（1440/390/360）· 令牌 64/64

> 承接 A4 退回批：C3 与 A9 本批完成；A 节残余 = A3 / A8 / 移动端 #techstack 遮挡（均待拍板）

## 🏷️ 2026-09-16 · A4 退回原版与项目成果字号放大

**结论**：A4 简介区 12 栅格（文字 6 列 + 照片 5 列右偏）经用户看效果**否决并退回原版**（单列：正文靠左 + 满宽 4:3 大图）；A6 项目卡并排保留，成果列表字号由 `--text-body` 提到 `--text-body-large`（与描述同级，1440 实测两者均 20px）。

| 维度 | 状态 |
|------|------|
| 主线 | ✅ `v3-full`（A4 退回为否决记录；A6 + 字号保留） |
| commit | `revert` + `fix`，见 git log |
| 遗留 | 本地未推送 commit 累积 · 线上仍为 v2.4 · 快照超限 → 最旧「全量对账」已归档 |

### ✅ 完成（Δ 自布局重排）

| 项 | 位置 | 说明 |
|----|------|------|
| A4 退回原版 | `desktop.css` | 删 ≥1024 的 12 栅格规则，恢复「正文靠左 + 满宽 4:3 大图」；CSS 注释已标「已否决，勿再提」 |
| 成果字号放大 | `desktop.css` | `.project-item__results li`：`--text-body` → `--text-body-large`；1440 下 20px = desc 同号（原 16px），1024 下同为 16px |
| 页高复测 | — | 1440×900＝**15120** · 390×844＝**14407** · 360×844＝**14633**（移动端 +75/+78px 全部来自成果字号，A4 退回**不影响**移动端） |

### 🧭 决策

| 决策 | 结论 | 沉淀 |
|------|------|------|
| A4 方向 | **否决「12 栅格 6+5」并退回原版**；勿再提该方向，如重做需新方案 | ✅ 已记 review-v3 二节 + CSS 注释 |
| 成果字号 | 取与描述**同级**（`--text-body-large`），不取中间值 | ✅ 已记 CLAUDE.md 交互设计 |
| 视觉改动流程 | 实施后**先出截图给用户确认**再收尾（B6/B7/A3/A4 四例均为量化验证通过后被否决） | ✅ 已补 CLAUDE.md 红线 #5 |

### 🔴 坑（勿重踩）

| 现象 | 根因 | 解决 | tag |
|------|------|------|-----|
| A4 通过了全部量化验证仍被否 | 验证只覆盖「并排成立 / 页高合理」，未覆盖「文字列收窄后的阅读观感」 | 视觉类改动除几何验证外**必须出截图先给用户看**，再做下一批 | #tag: design |

### 💡 关键发现

- #tag: design — 「头重脚轻」的诊断成立，但候选方案（文字列收窄）带来新代价（行宽变短、文字块变高）→ **视觉方案的代价常在原问题维度之外**；给候选时应并列 2 个方向供用户选
- #tag: 视觉验证 — 字号类改动会同时改变**移动端页高**（成果字 14→16px 使 390/360 各 +75/+78px），页高基线须同步三档

### 🚀 下会话指令

> 承接 A4 退回：A 节残余 = A3（待定方向）· A8/A9（形态）· 移动端 #techstack 遮挡；C 节与 A7 需方向决策

1. 待拍板（按 TODO 优先级）：**I4 视频自动播放**（P1）· **<768 移动端 kicker 遮挡** · A3 新方向 · A8/A9 形态 · A7/C2 · C1 数字面板 · **C4 IA（导航 5 项降 4 项）** · 项目卡片一句话目标 · I12/I13/I14/I15 · v3-full 上线
2. 已否决（勿再提）：**A4 简介区 12 栅格（新）** · A3 统一白底衬 · 通栏黑带 · 全站暗色 · 竖版视频 · char-matrix · B6/B7
3. 未做：跨浏览器（Safari / Firefox）· I11 / I13 / I14 / I15 · A3 / A7 / A8 / A9 本体
4. 复现：探针 `/tmp/probe/`（`a47.js` 本次验证：intro 恢复 + 字号同级 + 页高三档）
5. 基线：页高 **15120**（1440×900）· **14407**（390×844）· **14633**（360×844）· 令牌 64/64

## 🏷️ 2026-09-16 · 布局重排 A4 / A6 落地（简介区 12 栅格 + 项目卡并排）

**结论**：简介区「正文靠左 + 满宽大图」改为 12 栅格「文字 6 列 + 照片 5 列右偏」；项目卡描述与成果改并排双列（`.project-item` 三列 `132px | 1fr | 1fr`）。**仅 ≥1024 生效**，平板/移动与页高（14332 / 14555）零影响；桌面页高 15640 → **14257**（新基线）。

| 维度 | 状态 |
|------|------|
| 主线 | ✅ `v3-full` 布局重排（A 节再清 2 项） |
| commit | `feat`（A4/A6），见 git log |
| 遗留 | 本地未推送 commit 累积 · 线上仍为 v2.4 · 待拍板项见 TODO |

### ✅ 完成（Δ 自断点族）

| 项 | 位置 | 说明 |
|----|------|------|
| A4 简介区重排 | `desktop.css` | ≥1024：`.section__body` 12 栅格，文字 `1 / span 6`、照片 `8 / span 5`（第 7 列作空隙）；照片 4:3 由 1280×960 → 514.7×386（1440 实测） |
| A6 项目卡并排 | `index.html` `desktop.css` | 4 处「成果」段加 `.project-item__section--results`；`.project-item` 改 `132px 1fr 1fr`，desc `2/3`、成果 `3/4`；1440 实测 desc 右 786 < 成果左 818（并排成立） |
| 验证 | 探针 `a46.js` | 1440/1024 并排 ✓；900 平板（docH 13953）与 390（14332）、360（14555）**零变化** ✓ |

### 🧭 决策

| 决策 | 结论 | 沉淀 |
|------|------|------|
| A4 照片列宽 | 取 5 列 + 第 7 列空隙（而非 6 列贴边）—— 留呼吸空间、避免与左侧文字等宽 | ✅ 已记 CLAUDE.md 交互设计 |
| A6 生效断点 | 全桌面档（≥1024）对称双列，不设 1152/1280 二段 —— 简单可预期；1024 下 20 字/行偏窄但可读 | ✅ 已记 CLAUDE.md 交互设计 |
| 用类名而非 `:has()` | `--results` 显式标记，避免跨浏览器 `:has()` 支持差异（review 第 7 节未验） | — 会话内有效 |

### 🔴 坑（勿重踩）

| 现象 | 根因 | 解决 | tag |
|------|------|------|-----|
| `--results` 的 `grid-column` 一度会被基类压掉 | `.project-item__section--results` 与 `.project-item__section` **同特异性**，靠书写顺序取胜 | `--results` 规则必须写在基类**之后**（已在 CSS 注释注明） | #tag: css |

### 💡 关键发现

- #tag: css — grid 容器「加列 + 定点占用」是「两元素并排」的纯 CSS 解：容器从 2 列改 3 列，两个元素分别固定到 `2/3`、`3/4` 列，其余元素保持 `2 / -1` 跨列不受影响
- #tag: 视觉验证 — 布局重排会显著改变页高（本次 −1383px），桌面基线需当即更新；同时必须复测移动/平板确认「零影响」（A4/A6 只在 ≥1024，900/390/360 三档实测未变）

### 🚀 下会话指令

> 承接布局重排：A 节残余 = A3（待定方向）· A8/A9（形态）· 移动端 #techstack 遮挡（待拍板）；C 节（C1–C5）与 A7 需方向 / IA 决策

1. 待拍板（按 TODO 优先级）：**I4 视频自动播放**（P1）· **<768 移动端 kicker 遮挡**（含页高 +32px）· A3 新方向 · A8/A9 形态 · A7/C2 摘要+弹窗 · C1 数字面板 · **C4 IA（导航 5 项降 4 项）** · 项目卡片一句话目标 · I12/I13/I14/I15 · v3-full 上线
2. 已否决（勿再提）：A3 统一白底衬 · 通栏黑带 · 全站暗色 · 竖版视频 · char-matrix · B6/B7
3. 未做：跨浏览器（Safari / Firefox 复核 A4/A6 的 12 栅格与 3 列卡）· I11 / I13 / I14 / I15 · A7/A8/A9 本体
4. 复现：探针 `/tmp/probe/`（`a46.js` 布局重排验证 · `bp2.js` 断点巡检）
5. 基线：页高 **14257**（1440×900，新）· 14332（390）· 14555（360）· 令牌 64/64

## 🏷️ 2026-09-16 · 断点族落地（gutter 流式 + 标题连续化）

**结论**：A11 / A12 / A15 与「768–1023 导航压标题」一次修完 —— 新增 `--gutter` 三档统一入口（平板档流式 16→80px），hero 标题跨断点严格连续（1023 与 1024 同为 48px），平板档顶部留白 96→128px；**桌面 / 移动页高基准（15640 / 14332 / 14555）复测零变化**。

| 维度 | 状态 |
|------|------|
| 主线 | ✅ `v3-full` 断点族修复（A 节再清 3 项 + K5 遗留） |
| commit | `fix`（断点族），见 git log |
| 遗留 | 本地未推送 commit 累积 · 线上仍为 v2.4 · **新发现待拍板**：<768 移动端 #techstack kicker 被药丸遮（见 TODO P2） |

### ✅ 完成（Δ 自动效批收口）

| 项 | 位置 | 说明 |
|----|------|------|
| A12 gutter 流式 | `style.css` `desktop.css` `mobile.css` | 新增 `--gutter` 三档：<768 16px · 769–1023 `clamp(1rem, 25vw - 176px, 5rem)` · ≥1024 80px；`.container/.section/.hero/.footer/.term-divider` 全改引 `--gutter`（实测 769/820/900/1023 = 16.25/29/49/79.75px） |
| A11 标题连续化 | `desktop.css` | <1024 改 `min(--text-headline-2, (100vw − 2·gutter − 16px)/9, 3rem)`；1023px 71.6→**48px**，与 1024px 的 48px 严丝合缝（769–1023 全档实测 48px） |
| A15 孤字修复 | `desktop.css` | 360/375/390 字号 34.7/36.3/38px（原恒定 40px），9 字行宽均 < 列宽，无孤行 |
| 平板档顶部留白 | `desktop.css` | `.section` + `#techstack` 769–1023 走 `clamp(6rem, 12.5vw, 8rem)`（96.1→127.9px）→ kicker 较导航底边（84px）净空 12→44px；`#projects`（`padding-top:0` + scroll-margin 96）不受影响 |
| 断点边界堵缺口 | `desktop.css` | footer / hero__visual 的 `max-width: 1023px` → `1023.98px`（堵 1023–1024 分数宽度缺口） |

### 🧭 决策

| 决策 | 结论 | 沉淀 |
|------|------|------|
| 档位方案 | 选「加中间档 + 矢量流式」不重排全程 —— 改动面小（3 文件 +57/−24）、可回退 | ✅ 已记 CLAUDE.md 令牌表 + 交互设计 |
| 平板档 hero 收敛 | 为消除 −33% 跳变，769–1023 收敛到 48px（原 53.8–71.6px），换跨断点连续 | ✅ 已记 CLAUDE.md 交互设计 |
| tablet 档起点取 769px | 不动 mobile 的 `max-width: 768px`（768px 仍移动档）—— 避免连带变桌面 nav 的风险 | — 会话内有效 |

### 🔴 坑（勿重踩）

| 现象 | 根因 | 解决 | tag |
|------|------|------|-----|
| 差点漏改 `#techstack` 顶部 | 它有独立 ID 规则（64px），优先级高于 `.section` 类 —— 只按类改会漏 | 覆盖留白必须连带 ID 规则一起核对（已单独加平板档规则） | #tag: css |
| 探针 5 个输出全 40B：「node: 未找到命令」 | node 不在默认 PATH（nvm 装在 `~/.nvm`） | `export PATH="$HOME/.nvm/versions/node/v22.23.1/bin:$PATH"`（已记 review-v3 复现环境） | #tag: 视觉验证 |
| jump(#contact) 读数失真 | hash + smooth 滚动时长随距离增长，1.8s 读不全 | 改 `scrollIntoView({behavior:'instant'})` + 改用「kicker 相对 section 顶边偏移」静态判据 | #tag: 视觉验证 |

### 💡 关键发现

- #tag: css — 断点修复要连**同族量**一起看：同一断点上并存 gutter / 字号 / 顶部留白三类连续量，只修一类会在相邻量上留新阶跃
- #tag: 视觉验证 — 流式档的验证锚点 = **断点两侧取值相等**（1023↔1024：gutter 79.75↔80、padTop 127.875↔128、字号 48↔48）；比只看单档更有效
- #tag: docs — TODO 对账中「删除已完成项」宜整块替换，逐行改易留重复行（本轮发生过并已修正）

### 🚀 下会话指令

> 承接断点族：A 节残余 = A3（待定方向）· A4/A6（视觉方向）· A8/A9（形态）· 移动端 #techstack 遮挡（新待拍板）

1. 待拍板（按 TODO 优先级）：**I4 视频自动播放**（P1）· **<768 移动端 kicker 遮挡**（新，含页高基线 +32px 变更）· A3 新方向 · A8/A9 形态 · A4/A6 · 项目卡片一句话目标 · I12/I13/I14/I15 · C1/C2 · v3-full 上线
2. 已否决（勿再提）：A3 统一白底衬 · 通栏黑带 · 全站暗色 · 竖版视频 · char-matrix · B6/B7
3. 未做：跨浏览器（Safari / Firefox 需在用户机器补，复核 clamp 流式与 3rem 收敛）· I11 / I13 / I14 / I15 · A4/A6/A7/A8/A9 本体
4. 复现：探针 `/tmp/probe/`（`bp2.js` 断点巡检：gutter / 字号行分布 / kicker 遮挡判定）· node 需先 export PATH（见 review-v3 复现环境）
5. 基线：页高 15640 / 14332 / 14555 复测未变 · 令牌 64/64

