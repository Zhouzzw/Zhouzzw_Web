# PROGRESS.md — 个人网页项目进度

> progress 会话快照（Δ 增量）。静态基线见 CLAUDE.md；未决事项见 TODO.md；更早快照见 `docs/archive/`。

**当前**：v3-full 主线（v2 基础版已冻结；线上仍为 v2.4 https://zhouzzw.github.io/Zhouzzw_Web/）

## 📋 快照索引

| 日期 | 主题 | 摘要 | 状态 |
|------|------|------|------|
| 09-16 | 工程债清理、无障碍收口与生产复测 | 第 0.5 档 J1–J8+I10 + 第 1 档 A1 + I 节无障碍 I1/I6/I8/I9 全落地；性能数据迁到生产产物复测；A3 退回、播放键与留白调整 | ✅ 完成 |
| 09-16 | 全量可优化项对账与文档同步 | 补 review-v3 的 **J 节**（代码卫生 / SEO / 令牌债 J1–J8）、A·B·I 三表补状态标记、TODO 新增 P1 分组 | ✅ 完成 |

> 检索归档：`grep -n "#tag: <关键词>" docs/archive/*.md`。09-12「项目卡片分隔线」只提交未写快照（git `72254f6`）。

## 🏷️ 2026-09-16 · 工程债清理、无障碍收口与生产复测

**结论**：一口气清空 review-v3 的**第 0.5 档（J1–J8 + I10）**、**第 1 档（A1）**与**整个 I 节无障碍项（I1/I6/I8/I9）**，并把性能数据从 dev 迁到**生产产物**复测；A3 与另两处视觉方案经用户看效果后退回 / 重做。逐项实测证据见 `review-v3` **J 节 / K 节**。

| 维度 | 状态 |
|------|------|
| 主线 | ✅ `v3-full` 工程债清理 + 无障碍收口 |
| commit | `f0f7fab` · `a1d4cc1` · `551ab00` · `e25f894` |
| 遗留 | 🟡 本地 **5 笔未推送** · I 节仅剩 I4（待拍板）· 线上仍为 v2.4 |

### ✅ 完成（Δ 自全量可优化项对账与文档同步）

| 项 | 位置/commit | 说明 |
|----|------------|------|
| 第 0.5 档 J1–J8 + I10 | `f0f7fab` | 暗区去内联 `#242423` → `--color-black` · **令牌声明 60 / 引用 60，差集为空** · favicon（消 404）· `aria-current` · robots + sitemap · 死类 + 3 个死关键帧清空 · 内联 **29→5**（5 文件 **+134 / −601**）→ 细节见 review-v3 **J 节** |
| 第 1 档 A1 + I 节无障碍 | `a1d4cc1` | A1 左列 `100→132px`+`nowrap`（角色名全 1 行）· **I1** 封面改真 `<button>` · **I6** `aria-pressed`/`aria-hidden` · **I8** 3.01→**6.27:1** · **I9** 4.30→**4.99:1** → **全站文本达 AA** |
| 生产构建复测（H2-2） | `a1d4cc1` | dist 首屏 **2320.8 KB**（dev 2665.1）· JS **2.9 KB**（dev 250.8）· **CLS 0** · **I12 生产同样复现**（预取 ≈**4.2 MB**）· 新发现 **Noto Sans SC 19 个 woff2 子集 = 1082 KB** → 见 **K3** |
| A3 退回 + 播放键重做 | `551ab00` | 「9 图标统一白底衬」被**否决并退回**（同 run 对照 390/360 零位移）· 播放键纯白实心圆 → **暗色玻璃圆**（`rgba(13,13,15,.42)`+描边+`blur(4px)`）→ 见 **K4** |
| 导航↔标题间距 + 文档对账 | `e25f894` | `.section` 桌面顶部 `160→128px` → 导航底边到 kicker **76→44px** · 1440×900 页高 →**15640**，移动端零变化 · review-v3 新增 **K 节** + A·I·D·H 回写 → 见 **K5** |

### 🧭 决策

| 决策 | 结论 | 沉淀 |
|------|------|------|
| J2 暗色令牌 | 统一到既有 `--color-black`（`#232323`），**不新增** `--color-cement`（两者仅差 RGB(1,1,0)，统一后近似黑 4→3） | — 改 1 行可回退 |
| 死样式清理边界 | 删「被删组件的残骸」，保留「可复用原语」（`.sr-only`/`.container`/`.text-balance`/`.char-matrix*`/`.mono-label--accent`/`.stagger-1/3/4`/`.tok-*` 色板/`cursorBlink`） | — 会话内有效 |
| A3 统一白底衬 | **用户看效果后否决并退回**；勿再用「统一白衬」方向，如需处理先定新方向 | ✅ 已记 review A3 / TODO |

### 🔴 坑（勿重踩）

| 现象 | 根因 | 解决 | tag |
|------|------|------|-----|
| 整页高度跨 run 差 ≈50px，一度得出「390 由 14332→14377」的错误结论 | `.hero` 用 `100svh` / `90svh` → **页高依赖视口高**，`--h 900` 与 `844` 差 56px | 移动端锁定同一 `--h`；只信「同 run 内 base↔cur」的差值 | #tag: 视觉验证 |
| 导航底边到 kicker 的间距读成 92px（实为 76px） | `fadeUp` 含 `translateY(20px)`，IO 刚触发时量到的是动画中间态 | 读几何量前等入场动画结束（与 H3 对比度时序坑同源） | #tag: 视觉验证 |
| 视频首屏下载量测不出：RT 报 mp4 `transferSize=300` / `decodedBodySize=0`，CDP `encodedDataLength` 对被 abort 的 206 range 记 0 | 媒体走分段 range 请求且会被中断，两套口径都不累计 | 读 `video.duration` + `video.buffered` 时间区间按比例折算 | #tag: 性能 |
| 改 `<button>` 后每个视频封面矮 1.1px | `<p>` 降级为 `<span>` 后不再命中全局 `p { line-height: 1.6 }` | 该类显式补 `line-height` | #tag: css |


### 💡 关键发现

- #tag: css — **键盘可达性只能靠真实 CDP 键事件验证**：`<button>` 的 Enter/Space 激活发生在 **keypress**，只发 `keyDown` 不触发 click，须补 `type:'char', text:'\r'`。结果：封面进入 Tab 第 17/18/19 位 + Enter 成功开弹层
- #tag: css — 内联 > 类：`.section--dark { background }` 被 HTML 内联 `#242423` 静默压制了整轮 → **布局重排前必须先清内联**
- #tag: 性能 — 生产 vs dev 的差距几乎全在文本资源（js 250.8→**2.9 KB**、css 146.3→98.6）；**图片 1118 KB 与字体 1082 KB 字节完全不变，优化这两项才有意义**
- #tag: docs — `review-v3` 的 **I14 判据有误**：Geist 已在 `index.html` 引用（2 个 jsdelivr 样式表），本沙箱 `cdn.jsdelivr.net` 不可达才是根因 → 需正常外网复测才能定级
- #tag: 视觉验证 — `git worktree` 检出旧 commit 起第二个 dev server 做**同 run 对照**，是「清理 / 重构类改动零位移」最可靠的验证手段（据此确认 A3 精确退回）

### 🚀 下会话指令

> 承接全量可优化项对账：第 0.5 档 ✓ · 第 1 档 A1 ✓（A3 🚫 退回）· I 节无障碍 ✓ 收口（仅剩 I4）

1. 本地 **5 笔未推送**（`2a84314` / `f0f7fab` / `a1d4cc1` / `551ab00` / `e25f894`）→ push 待用户确认（不触发部署）
2. **第 2 档 B4 数字滚动计数** —— 无阻塞、与「量化严谨」最契合，review 推荐
3. **第 4 档 A4 / A6 布局重排** —— J1/J2 前置约束**已解除**，只差视觉方向
4. 待拍板：**I12 视频首帧（收益最大 ≈4.2 MB）** · **I4 自动播放** · A8/A9 形态 · **断点档位（A11/A12/A15 + 新发现的 768–1023 标题被导航压住）** · A4/A6 视觉方向 · A3 新方向 · 项目卡片一句话目标 · v3-full 上线方案
5. 未做：**H2-7 跨浏览器**（需在用户机器补 Safari / Firefox）· I11 · I13 · I15
6. 已否决（勿再提）：通栏黑带分隔条 · 全站统一暗色 · 竖版视频适配 · char-matrix 接入 · **B6 锚点描边** · **B7 Hero SCROLL 刻度** · **A3 统一白底衬**
7. 复现：探针已按 H3 重建在 `/tmp/probe/` —— `cdp.mjs` 新增 `--keys`（真实键序列）/ `--inject` / `--viewport` / `--clipY` / Network 域权威字节；另有 `geom.js` / `a11y2.js` / `i6.js` / `perf.js` / `space.js` / `icons.js`。**dev 5173 / preview 4173**

> 已沉淀 CLAUDE.md（**2026-09-16 用户确认后写入**，见新增节「## 工程红线」5 条）：① 断点对必须互为补集；② 用 `var()` 前确认声明存在并跑「引用 vs 声明」比对；③ 布局重排前先清内联样式（内联 > 类，会静默压制）；④ 页面高度依赖视口高度（hero 用 `svh`），跨 `--h` 不比绝对值 + 用 worktree 同 run 对照验证零位移；⑤ review 报告动手后必须回写状态标记。已否决项同指令 6。归档：本次把「D 节第 0 档五项修复落地（09-14）」与「动效批与联系方式微信模块落地（09-15）」移入 `docs/archive/`。

## 🏷️ 2026-09-16 · 全量可优化项对账与文档同步

**结论**：本会话未改生产代码（仅一笔联系方式顺序调整）。对 `review-v3` 与代码现状全量对账：补出 **J 节**（代码卫生 / SEO / 令牌债，J1–J8，全部 grep 实证），A / B / I 三表补状态标记，D 节重排（新增第 0.5 档），F 节标注已定项，TODO 新增 **P1 分组**。**下一会话从 J 节与「第 0.5 档」起手。**

| 维度 | 状态 |
|------|------|
| 主线 | `v3-full` |
| commit | `2039514`（联系方式顺序：GitHub → Email → Phone → WeChat，1 文件 +8 −8） |
| 遗留 | 🟡 本地 **3 笔未推送** · review 余项见 TODO.md · 线上仍为 v2.4 |

### ✅ 完成（Δ 自动效批与联系方式微信模块落地）

| 项 | 位置 | 说明 |
|----|------|------|
| 联系方式顺序调整 | `index.html` | 调整为 **GitHub → Email → Phone → WeChat**；仅交换两块，`first-child` / `last-child` 边距与分隔线自然跟随，零 CSS 改动；实测顺序与 href 均正确、整页高度不变 |
| 文档对账 | `docs/review-v3.md` `TODO.md` | 补齐 09-14 / 09-15 成果的状态标记（避免下轮重做）；新增 J 节 8 条工程债；TODO 新增 P1 分组；最旧快照归档 |

### 🔴 坑（勿重踩）

| 现象 | 根因 | 解决 | tag |
|------|------|------|-----|
| 文档里的待办与代码现状不一致，易重复劳动 | review 报告是「当时快照」，动手后未回写状态 | 每批落地后立即回写状态标记（✅/🟡/🚫）与已完成清单 | #tag: docs |
| 探针目录与 dev server 双双失效，无法实测 | `/tmp` 被系统清理、dev server 未常驻 | 需要实测时按 review-v3「H3 复现环境」重建探针；结论标注「未实测」而非据源码推断 | #tag: 视觉验证 |

### 💡 关键发现

- #tag: docs — 渲染 / 交互维度的 review 会漏掉「源码卫生」类问题；本次补 J 节后发现死样式规模（约 28 个类）远超原记的 I10 / I11 两处
- #tag: css — 令牌差集里 `--nav-height: 60px` 与实际写死的 52px（桌面）/ 44px（移动）**三方不一致**，是「令牌写着好看但没人引用」的典型
- #tag: css — 暗区背景被内联 `#242423` 硬编码 5 处，`.section--dark` 的 `background` 因此**从未生效**；这类内联债必须先清，否则后续布局重排要再动一遍
- #tag: design — 纯装饰动效的容忍度低于 review 的预期（B6 / B7 实现后被否）→ 后续提案先问再做

### 🚀 下会话指令

> 承接动效批：第 0 档已清空，本轮未写生产代码，起手点是 J 节与 D 节「第 0.5 档」

1. 本地 3 笔未推送（`b035a72` / `914801f` / `2039514`）→ push 待用户确认（不触发部署，不影响线上）
2. **第 0.5 档（J 节）优先**：J4 favicon · J6 `aria-current` → J1 + J2 令牌化（**必须先于 A4/A6 布局重排**）→ 清理批 J3 / J7 / J8 / I10，顺手 J5（robots + sitemap）
3. 待实测：`.contact-item-big` 无 `flex-wrap`，360px 下微信行（值 + 两枚 chip + 40px 图标）疑横向溢出 —— 起 dev server 测一次再定级
4. 待拍板：I4 自动播放 · I12 视频首帧 · A8 遮罩形态 · A9 ASCII 处置 · 断点是否加第三档 · A4/A6 视觉方向 · 项目一句话目标 · v3-full 上线 · J1/J2 令牌方案
5. 已否决（勿再提）：通栏黑带分隔条 · 全站统一暗色 · 竖版视频适配 · char-matrix · **锚点描边（B6）** · **Hero SCROLL 刻度（B7）**
6. 未做 —— 跨浏览器（Safari / Firefox）· 生产构建复测；**探针 `/tmp/probe/` 已被系统清理，需按 review-v3 H3 重建**
7. 归档 —— 本次把「P2 视觉与 P3 内容优化收尾」移入 `docs/archive/`

> 拟沉淀 CLAUDE.md（待确认）：① 断点对必须互为补集；② 用 `var()` 前确认声明存在并跑「引用 vs 声明」比对；③ **review 报告动手后必须回写状态标记**（否则下轮把已完成项当待办重做）。









