# PROGRESS.md — 个人网页项目进度

> progress 会话快照（Δ 增量）。静态基线见 CLAUDE.md；未决事项见 TODO.md；更早快照见 `docs/archive/`。

**当前**：v3-full 主线（v2 基础版已冻结；线上仍为 v2.4 https://zhouzzw.github.io/Zhouzzw_Web/）

## 📋 快照索引

| 日期 | 主题 | 摘要 | 状态 |
|------|------|------|------|
| 09-16 | 动效批收口与 review 瘦身 | B3/B4/B5 全落地并逐项实测（打字机 / 数字计数 / 跟随高光）；review-v3 345→130 行 | ✅ 完成 |
| 09-16 | 工程债清理、无障碍收口与生产复测 | 第 0.5 档 J1–J8+I10 + 第 1 档 A1 + I 节无障碍 I1/I6/I8/I9 全落地；性能数据迁到生产产物复测；A3 退回、播放键与留白调整 | ✅ 完成 |
| 09-16 | 全量可优化项对账与文档同步 | 补 review-v3 的 **J 节**（代码卫生 / SEO / 令牌债 J1–J8）、A·B·I 三表补状态标记、TODO 新增 P1 分组 | ✅ 完成 |

> 检索归档：`grep -n "#tag: <关键词>" docs/archive/*.md`。09-12「项目卡片分隔线」只提交未写快照（git `72254f6`）。

## 🏷️ 2026-09-16 · 动效批收口与 review 瘦身

**结论**：B 节剩余三项动效（B4 数字计数 → B3 Hero 打字机 → B5 跟随高光）同日全部落地、逐项实测通过，**B 节正式收口**；review-v3 按「只留未决事项」瘦身 **345 → 约 130 行**，TODO 指针同步。

| 维度 | 状态 |
|------|------|
| 主线 | ✅ `v3-full` 动效批收口（B1–B5 全落地；B6/B7 已否决） |
| commit | `feat`（B3/B4/B5）+ `docs`（瘦身与对账），见 git log |
| 遗留 | 🟡 本地未推送 commit 累积（含前批 5 笔）· 线上仍为 v2.4 · 剩余项均属「待拍板」 |

### ✅ 完成（Δ 自工程债清理、无障碍收口与生产复测）

| 项 | 位置 | 说明 |
|----|------|------|
| B4 数字滚动计数 | `index.html` `style.css` `animations.js` | 成果 6 数值包 `.js-count`（**终值即原文**，无 JS/爬虫/减动效零退化）；IO 触发 1.2s easeOutCubic，动画期锁 `min-width` + `tabular-nums` 防推挤；范围决策：描述段 `1.75s→0.21~0.60s` 保持静态（数到 1.75 强调旧值，语义反效果） |
| B3 Hero 终端打字机 | `index.html` `desktop.css` `animations.js` | DCL+700ms 起、90ms/字逐字打出 + 光标 `cursorBlink 1.1s`；当日两次用户调整后**定稿 `$ WHOAMI 周子惟`**（删 kicker 行、接位放大 `--text-body-large`、改大写、补 `'Noto Sans SC'` CJK 回退） |
| B5 卡片跟随高光 | `desktop.css` `main.js` `style.css` | `.chip-board .tech-cat` / `.project-item__image`；`@media (hover:hover)(pointer:fine)` 门控（JS 同口径，触屏零监听）；rAF 写 `--spot-x/--spot-y`；`::after` 圆角叠加层（不裁剪既有 img scale hover） |
| review-v3 瘦身 + 对账 | `docs/review-v3.md` `TODO.md` `CLAUDE.md` | 只留未决事项/已否决/已确认结论/复现环境；TODO 9 处失效指针改指新章节；令牌基线 60→62；交互设计补 B5 约定 |
| 探针扩能 | `/tmp/probe/cdp.mjs` | 新增 `--mouse`（真实鼠标事件）/ `--scrollInstant` / `--rm`（减动效仿真）/ `--touch`（触屏仿真）+ `--blink-settings` 强制媒体类型；`b5-diff.py` 像素差分 |

### 🧭 决策

| 决策 | 结论 | 沉淀 |
|------|------|------|
| B3 行内容二选一 | **`$ WHOAMI 周子惟`**（拉丁名与导航药丸重复，中文名才是 hero 新信息 + 「我是谁→中文回答」语义闭环） | — 会话内有效 |
| B5 叠加层实现 | `::after` **自带卡片同款圆角**，不用容器 `overflow:hidden` → 不裁剪既有 `img scale(1.01)` hover | ✅ 已记 CLAUDE.md 交互设计 |
| review-v3 定位变更 | 从「全量报告」改为**「未决事项 + 已否决 + 已确认结论 + 复现环境」**；已完成细节回落 PROGRESS/git | ✅ 已改 CLAUDE.md 红线 #5 措辞 |

### 🔴 坑（勿重踩）

| 现象 | 根因 | 解决 | tag |
|------|------|------|-----|
| B5 首测 `::after` 完全无样式、`--spot` 未写入（疑似代码坏） | **headless 默认 `(hover:none)(pointer:none)`** → CSS 媒体查询与 JS matchMedia 双门控静默不生效 | 探针启动参数强制 `--blink-settings` 桌面鼠标；另加 `--touch 1` 反向仿真触屏 | #tag: 视觉验证 |
| 跨 run 截图比对出 ~10px 整体假位移（峰值 235 的假 diff） | `--scroll` 用 smooth，悬停 run 多派 2 次鼠标事件晚 540ms 截图 → 滚动未完全落定 | 加 `--scrollInstant 1`（即时滚动），差分 bbox 随即精确吻合 | #tag: 视觉验证 |
| 探针 ERR_CONNECTION_REFUSED、静默测空 | URL 端口笔误（写 5174，dev 实际 5173）—— H3 已记「先 curl 确认端口」仍再犯 | 跑前先 `curl` 确认；已列入 review-v3 复现清单 | #tag: 视觉验证 |

### 💡 关键发现

- #tag: css — **交互效果（hover 类）必须 CSS 媒体查询 + JS matchMedia 同口径门控**，且只能靠**真实 CDP 鼠标事件**验证（合成 PointerEvent 不触发 `:hover`）；减动效仿真必须走 CDP `Emulation.setEmulatedMedia`，页面内 stub matchMedia 无效
- #tag: 性能 — 指针跟随高光用「rAF 节流 + CSS 变量 + `::after` 径向渐变」实现零布局成本（`--spot-x/--spot-y` 需先在 `:root` 声明，红线 #2）
- #tag: 视觉验证 — **hero 是 `svh` 垂直居中，内容增减被视口高度吸收**：B3 加一行、字号放大一档，页高基准（15640/14332）均零变化；像素差分 + bbox 与元素矩形对账是「局部装饰效果」最硬的证据形式
- #tag: docs — review 报告会随批次膨胀；**已完成项细节回落 PROGRESS/git，正文只留未决点**（345→130 行），但删除章节会让历史快照的「见 K3/K4/K5」指针悬空 → 需在文档头声明回溯方式并同步 TODO 指针

### 🚀 下会话指令

> 承接 review 瘦身：B 节全清，起手点在「断点族（A11/A12/A15 + 768–1023 压标题）」与「A4/A6 视觉方向」

1. 本地未推送 commit 累积 → push 待用户确认（不触发部署，不影响线上）
2. 待拍板（详见 review-v3 一/二节 + TODO）：断点档位决策 · A4/A6 视觉方向 · A8/A9 形态 · A3 新方向 · I4 自动播放 · **I12 视频首帧（收益最大 ≈4.2MB）** · C1/C2 是否推进 · 项目卡片一句话目标 · v3-full 上线
3. 已否决（勿再提）：通栏黑带 · 全站统一暗色 · 竖版视频适配 · char-matrix · B6 锚点描边 · B7 Hero 刻度 · **A3 统一白底衬**
4. 未做：跨浏览器（需在用户机器补 Safari / Firefox）· I11 / I13 / I14 / I15 · A4 / A6 / A7 / A8 / A9 / A11 / A12 / A15 本体
5. 复现：探针 `/tmp/probe/` —— `--mouse` / `--scrollInstant` / `--rm` / `--touch`；**headless 需 `--blink-settings` 才认 hover 媒体查询**；细节见 review-v3「复现环境」

## 🏷️ 2026-09-16 · 工程债清理、无障碍收口与生产复测

**结论**：一口气清空 review-v3 第 0.5 档（J1–J8 + I10）、第 1 档（A1）与 I 节无障碍（I1/I6/I8/I9），并把性能数据迁到**生产产物**复测；A3 与两处视觉方案经用户看效果后退回 / 重做。

| 维度 | 状态 |
|------|------|
| 主线 | ✅ `v3-full` 工程债清理 + 无障碍收口 |
| commit | `f0f7fab` · `a1d4cc1` · `551ab00` · `e25f894` |
| 遗留 | 本地未推送（已并入后续批次）· I 节仅剩 I4 · 线上仍为 v2.4 |

### ✅ 完成（Δ 自全量对账）

| 项 | 位置 | 说明 |
|----|------|------|
| 第 0.5 档 J1–J8 + I10 | `f0f7fab` | 暗区去内联 → `--color-black`；令牌差集归零；favicon；`aria-current`；robots + sitemap；死类 / 死关键帧清理；内联 29→5（5 文件 +134/−601） |
| 第 1 档 A1 + I 节无障碍 | `a1d4cc1` | A1 左列 132px + nowrap；I1 封面改真 `<button>`；I6 `aria-pressed`/`aria-hidden`；I8 6.27:1；I9 4.99:1 → **全站文本达 AA** |
| 生产构建复测 | `a1d4cc1` | dist 首屏 2320.8 KB（dev 2665.1）· JS 2.9 KB · CLS 0 · **I12 生产同样复现**（视频预取 ≈4.2 MB）· 新发现 Noto Sans SC 19 个子集 = 1082 KB |
| A3 退回 + 播放键重做 | `551ab00` | 「9 图标统一白底衬」被用户**否决并退回**；播放键改暗色玻璃圆 |
| 导航↔标题间距 + 文档对账 | `e25f894` | `.section` 桌面顶部 160→128px（导航底边到 kicker 76→44px）；1440 页高 → **15640**，移动端零变化 |

### 🧭 决策

| 决策 | 结论 | 沉淀 |
|------|------|------|
| J2 暗色令牌 | 统一到既有 `--color-black`，**不新增** `--color-cement` | — 改 1 行可回退 |
| 死样式清理边界 | 删「被删组件的残骸」，保留「可复用原语」（完整清单见 review-v3 第三节） | — 会话内有效 |
| A3 统一白底衬 | **用户看效果后否决并退回**；勿再用该方向 | ✅ 已记 review-v3 二节 |

### 🔴 坑（勿重踩）

| 现象 | 根因 | 解决 | tag |
|------|------|------|-----|
| 整页高度跨 run 差 ≈50px，误判「390 由 14332→14377」 | `.hero` 用 `svh` → 页高依赖视口高 | 锁定同一 `--h`；只信同 run 内 base↔cur 差值 | #tag: 视觉验证 |
| 导航到 kicker 的间距读成 92px（实为 76px） | `fadeUp` 含 `translateY(20px)`，量到动画中间态 | 读几何量前等入场动画结束 | #tag: 视觉验证 |
| 视频首屏下载量测不出 | 媒体走分段 range 且会被中断，RT 与 CDP 口径都不累计 | 读 `video.duration` + `buffered` 按比例折算 | #tag: 性能 |
| 改 `<button>` 后每个视频封面矮 1.1px | `<p>` → `<span>` 后不再命中全局 `p { line-height: 1.6 }` | 该类显式补 `line-height` | #tag: css |

### 💡 关键发现

- #tag: css — 键盘可达性只能靠**真实 CDP 键事件**验证：`<button>` 的 Enter/Space 激活发生在 keypress，只发 keyDown 不触发 click
- #tag: css — 内联 > 类：`.section--dark { background }` 被 HTML 内联 `#242423` 静默压制整轮 → **布局重排前必须先清内联**
- #tag: 性能 — 生产 vs dev 的差距几乎全在文本资源（js 250.8→2.9 KB）；**图片 1118 KB 与字体 1082 KB 字节不变，优化这两项才有意义**
- #tag: docs — review-v3 的 I14 判据有误：Geist 已在 `index.html` 引用，本沙箱 jsdelivr 不可达才是根因
- #tag: 视觉验证 — `git worktree` 检出旧 commit 起第二个 dev server 做**同 run 对照**，是「清理 / 重构类改动零位移」最可靠的验证手段

### 🚀 下会话指令

> 承接全量对账：第 0.5 档 ✓ · 第 1 档 ✓（A3 🚫）· I 节无障碍 ✓（仅剩 I4）。**上述后续批次已在其后快照执行完毕**，剩余待拍板项统一见 review-v3 一 / 二节与 TODO.md。

> 已沉淀 CLAUDE.md「工程红线」5 条（2026-09-16 用户确认后写入）；归档：「D 节第 0 档五项修复」与「动效批与联系方式微信模块」已移入 `docs/archive/`。

## 🏷️ 2026-09-16 · 全量可优化项对账与文档同步

**结论**：本会话未改生产代码（仅一笔联系方式顺序调整）。对 `review-v3` 与代码现状全量对账：补出 J 节（代码卫生 / SEO / 令牌债，J1–J8，全部 grep 实证），A / B / I 三表补状态标记，D 节重排（新增第 0.5 档），TODO 新增 **P1 分组**。

| 维度 | 状态 |
|------|------|
| 主线 | `v3-full` |
| commit | `2039514`（联系方式顺序：GitHub → Email → Phone → WeChat，1 文件 +8 −8） |
| 遗留 | 本地未推送（已并入后续批次）· 线上仍为 v2.4 |

### ✅ 完成（Δ 自动效批）

| 项 | 说明 |
|----|------|
| 联系方式顺序调整 | 交换两块，「微信」移末位；`first-child` / `last-child` 边距与分隔线自然跟随，零 CSS 改动，页高不变 |
| 文档对账 | 补齐 09-14 / 09-15 成果状态标记（避免下轮重做）；新增 J 节 8 条工程债；TODO 新增 P1 分组；最旧快照归档 |

### 🔴 坑（勿重踩）

| 现象 | 根因 | 解决 | tag |
|------|------|------|-----|
| 文档里的待办与代码现状不一致，易重复劳动 | review 是「当时快照」，动手后未回写状态 | 每批落地后立即回写状态标记并同步 TODO | #tag: docs |
| 探针目录与 dev server 双双失效，无法实测 | `/tmp` 被系统清理、dev server 未常驻 | 需要实测时按 review-v3「复现环境」重建；结论标注「未实测」而非据源码推断 | #tag: 视觉验证 |

### 💡 关键发现

- #tag: docs — 渲染 / 交互维度的 review 会漏「源码卫生」类问题；J 节补出后发现死样式规模（约 28 个类）远超原记
- #tag: css — `--nav-height: 60px` 与实际写死的 52px（桌面）/ 44px（移动）三方不一致；暗区背景被内联 `#242423` 硬编码 5 处，`.section--dark` 因此从未生效
- #tag: design — 纯装饰动效的容忍度低于 review 预期（B6 / B7 实现后被否）→ **后续提案先问再做**

### 🚀 下会话指令

> 第 0.5 档已清空，本轮未写生产代码，起手点是 J 节与 D 节「第 0.5 档」—— **已在其后批次全部执行完毕**；剩余项见最新快照与 TODO.md。
