# 2026-09-12 · P2 视觉与 P3 内容优化收尾

> 归档自 PROGRESS.md —— 快照超 3 个上限时移入。内容原样保留，可继续 `grep -n "#tag: <关键词>" docs/archive/*.md` 检索。

## 🏷️ 2026-09-12 · P2 视觉与 P3 内容优化收尾

**结论**：P2 视觉三项、P3 的 11/12/13 与荣誉等级列排版全部落地 —— 分隔条去底色与导航药丸脱钩、螺旋外圈衰减、项目卡片块顺序统一、荣誉与实习行右侧死区清零、区块 kicker 加序号。可自主处理的缺陷类问题（P0–P3）至此清空，剩余仅等素材 / 等文案 / 等决策。

| 维度 | 状态 |
|------|------|
| 主线 | `v3-full` |
| commit | `e770671`（本次共 10 笔） |
| 遗留 | 🟡 本地 16 笔未推送 · 等素材与等文案项见 TODO.md · 线上仍为 v2.4 |

### ✅ 完成（Δ 自 信息架构重组 + 终端分隔条定稿与迁移）

| 项 | 位置 | 说明 |
|----|------|------|
| 分隔条去底色 + 螺旋外圈衰减 | `style.css` | 分隔条去掉填黑与阴影、只留上下细线（与导航药丸同为深色圆角横条，材质上分开）；螺旋 r1–r4 透明度 0.18/0.38/0.62/0.84，消除硬裁切边，环数与旋转不变 |
| 技术栈标签 + kicker 序号 + 荣誉等级排版 | `index.html` `desktop.css` | `TECH STACK` → `OVERVIEW` 消同屏重复；kicker 加序号 `01 / ABOUT` … `07 / CONTACT`（沿用零补位编号语言）；荣誉等级定稿 22px / `--weight-medium` / 正文色 |
| 荣誉 / 实习行结果列 | `index.html` `desktop.css` | 新增 `.timeline-list__award`：荣誉放奖项等级或成绩、实习放角色；实测死区 45% → 0px |
| 项目卡片块顺序 | `index.html` | 4 卡原本三种不一致顺序，统一为 `title → desc → 成果 → tags → 图片 →（演示视频）`，量化结果前置 |

### 🧭 决策

| 决策 | 结论 |
|------|------|
| 分隔条与导航如何脱钩 | 改分隔条（降为结构线），不动导航 |
| 荣誉等级配色 | 先做「颜色承载等级」，用户改判为**统一黑色**，靠字号 / 字重制造权重 → 最终 22px / medium / 正文色 |
| 结果列语义 | 荣誉 ＝ 奖项等级或成绩（`二级` / `462 分`），实习 ＝ 角色；数据从现有标题拆出，不新增事实 |
| 案例结构改法 | 只做可量化的块顺序重排；「一句话目标」需用户文案，不代拟 |
| 区块头部同构 | 不做样式差异化，改加序号（沿用既有编号语言） |

### 🔴 坑（勿重踩）

| 现象 | 根因 | 解决 | tag |
|------|------|------|-----|
| 批量改行时后面的区间全部错位 / 越界 | 边遍历边切片，前面的改动使后继索引失效 | 从后往前处理 | #tag: docs |
| `git add -A` 把临时验证页一并提交 | 验证页建在仓库根目录且未忽略 | `git rm --cached` 撤销后加入 `.gitignore`（未用 amend） | #tag: git |
| 600 字重实际渲染得比预期粗 | 页面只加载 Noto Sans SC 的 400 / 500 / 700，600 按匹配规则落到 700 | 直接选用已加载档位（500 / 700） | #tag: css |

### 💡 关键发现

- #tag: 视觉验证 — 用 `Range.getBoundingClientRect()` 量「文字实际最右端 vs 行右边界」，可把「右侧空不空」这类主观判断变成可验证数字（本次据此证明死区为 0）
- #tag: design — 同一视觉槽位出现在相邻两区块时不该给不同处理；先统一语义，再决定是否需要分层
- #tag: css — 中文字重受实际加载字重集限制，写 600 不等于渲染 600

### 🚀 下会话指令

> 承接本轮优化收尾：可自主处理的缺陷类问题已清空，剩余全部依赖素材 / 文案 / 决策

1. 本地 16 笔未推送 → push 待用户确认（`v3-full` 不触发部署，不影响线上）
2. 素材类（工业卡片图 / 视频、全向轮视频）→ TODO.md P2，等用户提供
3. 项目区浏览成本偏高（占全站 42.9%、单卡 ~1570px）→ TODO.md P3，与 P4「卡片详情弹窗」同向，可合并推进
4. 项目卡片缺「一句话目标」→ TODO.md P3，等用户文案
5. 全局图片 `<picture>` + WebP → TODO.md P3
6. v3-full 上线方案（deploy workflow 只监听 `v2`）→ TODO.md P4，等用户决策
7. 已否决项 —— 通栏黑带分隔条 · 全站统一暗色 · 竖版视频适配 · char-matrix 接入
8. 归档 —— 本次将「v3-full 独立化：v2/v3 并列」移入 `docs/archive/`
