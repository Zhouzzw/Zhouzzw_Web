# v3-full 全站 Review（桌面 1440 + 移动 390 实测 · 含 4 个参考站正文核验）
> 覆盖范围：桌面 1440×900（整页 2 段 + 逐屏 20 张，实际逐张核对）+ 移动 390×844（整页 2 段 + 定点 6 张）+ **断点 11 档**（360 / 375 / 390 / 768 / 820 / 900 / 1023 / 1024 / 1025 / 1280 / 1440）；参考站 4 个已抓正文。
> 修订：2026-09-12 第二轮补证 —— 修正 A5（推翻）、新增 A8/A9、重写 E 节、补 G 证据等级审计与 H 交接清单。
> 修订：2026-09-12 **第三轮（H2 清单执行）** —— 新增 A10–A16（渲染 / 断点维度，实测 360/375/390/768/820/900/1023/1024/1025/1280/1440 共 11 档）、新增 **I 节**（交互 / 无障碍 / 性能维度，I1–I15 逐项实测）、G 节补新条目与「复核通过项」、H 节重写为「已完成 / 余项」。**H2-7 跨浏览器未完成**（本环境 Firefox 为 snap 包受沙箱限制且已有用户实例，未强行启动）。
> 修订：2026-09-14 **D 节第 0 档落地** —— A10 · A14（连带 A13）· A16 · I5 · I7 五项修复并实测通过（commit `feef54b`）。
> 修订：2026-09-15 **动效批 + 联系方式模块** —— B1 / B2 落地（连同 A2 一并清掉）· I2 弹层语义与焦点管理落地 · I4 补 `playsinline`（自动播放策略未决）；**B6 / B7 用户看后否决并已删除代码**。
> 修订：2026-09-16 **第四轮（源码卫生补充）** —— 新增 **J 节**（代码卫生 / SEO 完整性 / 令牌债，J1–J8，全量 grep 实证）；A / B / I 各表补状态标记；D 节执行顺序重排（新增第 0.5 档）；F 节标注已定项。
> **状态标记**（截至 2026-09-16）：✅ 已修 · 🟡 部分已修 · 🚫 已否决（勿再提）· **未标记 = 未动手**。已完成项的原「处置」列保留作历史依据，勿重做。
> ✅ **交接提示**：**下一会话优先读 J 节与 D 节「第 0.5 档」**；G 节是证据等级审计，H3 是环境与探针踩坑（**探针在 `/tmp/probe/`，会被系统清理，丢失需按 H3 重建**）。

## A. 确证缺陷（实测可见）

| # | 现象 | 根因 / 位置 | 处置 |
|---|------|------|------|
| A1 | 机械臂运控负责人折成两行 | 左列仅 100px | 左列 100→132px + nowrap |
| A2 ✅ | 全站无滚动入场动效 | desktop.css:513 禁用了 .animate-on-scroll | **已于 2026-09-15 完成**：长格式补 `fadeUp` + 各级 `.stagger-*`，`html.anim` 由 head 内联脚本同步添加（无 JS 时正文可见）；见 B1 |
| A3 | 工具箱 9 图标底衬不统一 | 仅 GitHub/ROS2 有白底圆角块 | 统一同尺寸底衬 |
| A4 | 简介区头重脚轻 | 正文 65ch 靠左 + 下接满宽 4:3 大图 | 改 12 栅格：文字 6 列 + 照片 5 列右偏 |
| A5 | ~~荣誉行右侧 30% 断层~~ **复核不成立** | 原判断纯属 CSS 推导 | 等级列已右对齐容器边，属正常表格行；可加点线填充中段（打磨项）|
| A6 | 项目描述右侧约 40% 空 | desc max-width:70ch，成果列表另起一段 | 描述与成果改并列双列 |
| A7 | 项目区占全站 42.9%（15828px 中约 6790px） | 4 卡纵向全展开 | 与 TODO P3/P4「摘要+详情弹窗」合并推进 |
| A8 | 移动端筛选器无滚动提示 | .tech-filter 在 <768px 为 overflow-x:auto + nowrap | 加右侧渐隐遮罩，露出下一项一角 |
| A9 | 页脚 ASCII 品牌字实际不可读 | 桌面 6px / <480px 直接 display:none（mobile.css:243）| 提至 8–9px + 0.18 不透明度，或换成可读徽标 |
| **A10** ✅ | **1024px 整点像素：右半屏深色面板出现，但螺旋装饰被隐藏 → 一块空黑块** | `desktop.css:252` 的 `min-width:1024px{…display:block}` 与 `desktop.css:1557` 的 `max-width:1024px{display:none}` 在 **1024px 同时命中**，后者源码顺序更靠后 → 覆盖 | 把 1557 的 `max-width: 1024px` 改为 `1023px`（或统一写成 `not (min-width:1024px)`）。**iPad Pro 12.9″ 竖屏正好 1024 CSS px，真机命中** |
| **A11** | **Hero 标题字号随视口变宽反而变小**：1023px＝71.6px → 1024px＝48px（−33%） | <1024 走 `--text-headline-2` ＝ `clamp(2.5rem, 7vw, 5rem)`（`style.css:40`）—— 1023px 时正好落在 7vw 段＝71.61px；≥1024 被 `@media (min-width:1024px)` 覆盖成 `min(--text-headline-2, calc((50vw − 80px)/9))`（`desktop.css:205`），1024px 时右项 (512−80)/9 ＝ 48px 胜出 → **两套算法在断点处不连续** | 取两段都连续的形式，或直接把列宽公式下推到全宽度区间 |
| **A12** | **768–1023px 平板区间左右留白骤降到 16px**：1023px 正文列 991px 几乎贴边，1024px 突变 80px | `.section` 基线是 `padding: … var(--gutter-mobile)`（16px），只有 `≥1024` 才切 `--gutter-desktop`（80px）；中间 255px 宽区间无人接管 | 给 768–1023 加中间档（如 `--gutter-tablet: 2rem`），或把 `--gutter-desktop` 提前到 768px 并做流式插值 |
| **A13** ✅ | **360px 导航溢出，末项「关于」被裁掉约一半** | 5 个导航项总宽 334px > 导航内容宽 326px（`navOverflow sw334/cw326`）；375px 时刚好放得下（351 < 359） | 修 A14 即可解决（隐藏「首页」腾出 ≈38px）；否则缩小 `.nav__links a` 内边距 |
| **A14** ✅ | **`<480px` 隐藏「首页」的规则是死规则，从未生效** | `mobile.css:235` 写 `li:nth-child(1)`，但 `.nav__links` 的第一个子元素是 `<span class="nav__links-indicator">`（该 span 直接放进 `<ul>` 本身也是非法 HTML），首个 `<li>` 实际是 `:nth-child(2)` → 选择器匹配不到任何元素 | 改 `li:nth-of-type(1)`（或 `li:first-of-type`）；顺带把 indicator 移出 `<ul>` 或包进 `<li>` 里 |
| **A15** | **360–375px Hero 标题孤字**：「上下位机全栈工程师」的「师」独占第三行 | `--text-headline-2` 的 clamp 下限 2.5rem＝40px 在 <571px 视口恒定，未随列宽收缩；9 个全角字 ×40px − 字距 ≈349px ＞ 360px 视口的 328px 列宽 | 与 A11 同源：把「字号由列宽决定」的思路下推到 <1024（如 `min(40px, calc((100vw − 32px)/9.2))`） |
| **A16** ✅ | **3 个 CSS 变量被 `var()` 引用但从未声明 → 属性静默失效**（不报错、不告警，全量扫描 63 个 `var()` 用法 vs 78 个声明得出） | ①`--color-muted`（desktop.css:1411）→ 弹窗关闭按钮 `color` 无效，回退继承 `#1A1A1A`，落在深色遮罩上实测 **1.06:1，不可见**；②`--radius-md`（desktop.css:1402 / 1425）→ 弹窗与视频封面圆角失效（回退 0）；③`--space-5`（desktop.css:821）→ `.tech-cat__list li` 的 `padding-left` 失效（回退 0），列表项与「·」标记的缩进丢失 | 三个都补成既有档位：`--color-muted` → 新建（如 `#8A8A8A`）或改用 `--color-ghost-grey`；`--radius-md` → `--radius-8`；`--space-5` → `--space-6`。**另建议加一条 lint**（扫描未声明变量），否则同类问题会再犯 |

## B. 动效与交互提案（按性价比排序）

| 优先 | 提案 | 说明 |
|---|---|---|
| B1 ✅ | 恢复滚动入场动效 | 删 desktop.css:513 禁用块；opacity + translateY(24px) + stagger；prefers-reduced-motion 已兼容。**2026-09-15 落地**（与 A2 同一批） |
| B2 ✅ | 顶部滚动进度条 | 2px 橙线随滚动填充，终端美学、零侵入。**2026-09-15 落地**，实测 35% 进度 → `matrix(0.34999)` |
| B3 | Hero 终端命令打字机 | kicker 下加 $ whoami 逐字输出 + 光标闪烁（cursorBlink 关键帧已存在） |
| B4 | 数字滚动计数 | 40% / 5× / 90.9% / 1.75s→0.21s 进入视口时从 0 计数，强化量化说服力 |
| B5 | 卡片鼠标跟随高光 | radial-gradient 跟随 pointer，用于 .chip-board .tech-cat 与 .project-item__image |
| B6 🚫 | 锚点落点高亮反馈 | :target 时区块标题短暂橙色描边，缓解长页跳转后「找不到落点」。**2026-09-15 实现后用户看后要求删除**，代码与 `--target-ring` 变量已全部移除 → 落点反馈改由导航药丸 scrollspy 承担，**勿再提议** |
| B7 🚫 | Hero Scroll 刻度可点击 | 现仅 ≥1024px 展示，加点击跳转下一区块。**2026-09-15 改造后用户仍要求删除整块**（标记 + 样式 + `heroScrollCue` 关键帧已清）—— 删除后螺旋圆环恢复连续，勿再恢复 |

## C. 更完整 / 更高级的新模块提案

| 优先 | 模块 | 理由 |
|---|---|---|
| C1 | 能力数字面板 metrics strip | 4 项：2 款竞赛机器人 / 8 大技术域 / 90.9% 到位率 / 3 项国奖。置于简介之后、技术栈之前，把量化提到第二落点 |
| C2 | G1-D 项目时间轴（2026.03→2026.10） | 把 003/004 两条路线串在时间轴上，体现「实习 → 落地」主线 |
| C3 | 技能掌握度条（熟练/熟悉/了解三档） | 技术栈现为标签墙，缺深度信号；**需你确认档位，不自拟** |
| C4 | 经历/荣誉合并为一条纵向时间线 | 实习(04)+荣誉(05) 合为 2025→2026 时间线，导航从 5 项降为 4 项；**改信息架构，需确认** |
| C5 | 博客/笔记页（第 5 页） | 已在 TODO P4，非本轮范围 |

## D. 建议执行顺序

0. ~~**第 0 档**~~ ✅ **已完成 2026-09-14**（commit `feef54b`）：A10 · A14（连带 A13）· A16 · I5 · I7 五项全部落地并实测通过
0.5 **第 0.5 档（2026-09-16 新增，J 节，仍是一行级 / 零视觉重设计，建议先清）**：J4 favicon · J6 `aria-current` → J1 + J2 令牌化（**必须排在 A4 / A6 布局重排之前**，否则重排要再动一遍内联值）→ 清理批 J3 + J7 + J8 + I10，顺手做 J5（robots + sitemap）
1. A1 / A3 / A8 / A9 —— 剩余确证缺陷（**A2 已于 2026-09-15 完成**；A5 复核不成立，剔除）；A8 / A9 需先定形态
2. ~~B1 + B2 + B4~~ → ✅ **B1 / B2 已完成（2026-09-15）**；**B6 / B7 已否决**；剩 B3 / B4 / B5 未做（其中 B4 数字计数与「量化严谨」最契合，推荐）
3. C1 metrics strip —— 新模块，纯增量、不动现有 IA（**无行业先例，属差异化尝试**，见 E 节）
4. A4 / A6 —— 布局重排，需先确认视觉方向（**建议排在 J1 / J2 令牌化之后**）
5. A11 / A12 / A15 —— 字号与留白的断点连续性，属同一族问题，建议一次改完（涉及 3 个断点的取值决策）
6. A7 / C2 —— 与 TODO P3/P4「摘要 + 详情弹窗」同向，可合并推进
7. I 节交互 / 无障碍 / 性能项 —— 按分组推进：**I-1 键盘可达性（I1 仅剩 1 项）→ I-2 语义与状态（I6 / I8 / I9）→ I-3 死代码（I10 / I11）→ I-4 性能（I12–I15）**；I2 / I3 / I5 已完成，I4 剩「自动播放策略」未决

## E. 设计参考（本次已抓取正文实证）

**E1 宇树 unitree.com** — Hero 为 10 屏轮播（主视觉 + 型号 + slogan + Learn More 四段式）；模块序：产品卡片网格 → NEWS CENTER → 大事记跑马灯 → 合作伙伴。可借鉴「Hero 承载多条主张」，但个人站无产品线，宜单屏。
**E2 智元 agibot.com.cn** — 首屏 4 条 Banner 承担「里程碑式传播」（量产 15000 台 / 赛事双第一 / 全球首个具身乐园）；结构＝大模型 → 三智一体三支柱 → 产品 6 卡 → 资讯。**未发现数字看板**。
**E3 云深处 deeprobotics.cn** — Hero 双按钮（我要订购 / 观看视频）；行业应用六宫格 → 产品中心 → 合作伙伴 logo 墙 → 资讯 6 条。**未发现数字看板**，里程碑放在 about 独立页（#comp）。
**E4 Awwwards SOTD《Robot》**（2025-07-16，Malvah，7.42/10）— 极端双色 #121212 + #FFF；Interactive Type 交互式排版 + 文字跑马灯 + 鼠标交互动效 + 视频化导航；短板：可用性 6.99、Accessibility 6.00。

**对提案的修正（依据本次实证）**

- ✅ 「首屏强主张 + 视频前置」成立 → 现有 Hero 方向正确
- ✅ 「大事记/里程碑为独立模块」在三家均有先例 → C2 项目时间轴有据可依
- ❌ 「数字面板是国内机器人官网共性」**被推翻**：智元、云深处均无 → C1 metrics strip **无行业先例**，属差异化尝试而非跟随惯例
- ⚠️ 纯双色 + 大字排版牺牲可用性（SOTD 可用性 6.99 / Accessibility 6.00）→ 本项目保留橙色点缀与骨架导航更稳

## F. 待确认（本轮未动手）

- C3 技术掌握度分档、C4 导航合并会改信息架构，且 C4 与 CLAUDE.md 已记录的「荣誉并入实习竞赛」决策相关，须先确认
- 素材类阻塞项（工业卡片实拍图/视频、全向轮视频）与 v3-full 上线方案仍见 TODO.md P2 / P4
- ~~**A13 / A14 的处置方向**~~ ✅ **2026-09-14 已定**：选「隐藏首页」—— 小屏少一个回顶入口（用户未反对），代价是 ≤480px 页顶 scrollspy 指向被隐藏项（见 TODO.md P3）
- **A15 / A11 / A12 的断点取值**（仍待定）：字号与留白公式是否给 768–1023 单独一档（引入第三个 `--gutter-tablet` 与第二段字号公式），还是一次性改成全程流式
- ~~**I7 橙色正文的处置**~~ ✅ **2026-09-14 已定**：保留橙色语义，新增 `--color-accent-deep: #A05200`（4.90:1）。如需改判为「降级为非正文」，一行可换
- **I12 视频首帧策略**（仍待定）：保留自动截帧（首屏即下载 4.48MB）还是改静态封面图（需补 3 张封面，`TODO.md P2` 素材类）
- **I4 自动播放策略**（仍待定）：加 `muted` 自动播（但用户看演示视频多半要声音）还是接受「需再点一次 play」
- **A8 遮罩 / A9 页脚 ASCII 处置**（仍待定）：渐隐遮罩的强度；ASCII 是「提到 8–9px」还是「换可读徽标」
- **A4 / A6 视觉方向**（仍待定）：简介区头重脚轻、项目描述右侧约 40% 空的布局重排方向
- **项目卡片「一句话目标」文案**（仍待定）：不代拟，需你提供
- **J 节 J1 / J2 的令牌方案**（仍待定）：暗色统一到 `--color-black`（`#232323`）还是新增 `--color-cement`（`#242423`）保留现有观感

## G. 证据等级审计（本轮补证后）

口径：**实证** = 已看该处截图 / 已抓该站正文；**代码推导** = 仅由源码推断未看渲染；**经验判断** = 无本项目亦无参考站证据。

| 条目 | 等级 | 依据 |
|---|---|---|
| A1 角色折行 | 实证 | 1440px 截图 step-10 / step-12 |
| A2 入场动效被禁用 | 实证 | desktop.css:513 + 截图无入场过程 |
| A3 工具箱图标底衬不统一 | 实证 | 1440px 截图 step-05 |
| A4 简介区头重脚轻 | 实证 | 1440px 截图 step-01 ~ step-03 |
| A5 荣誉行 30% 断层 | **实证后推翻** | 补看 y12150 截图：等级列已右对齐，非缺陷 |
| A6 项目描述右侧 40% 空 | 实证 | 1440px 截图 step-09 / step-11 |
| A7 项目区占 42.9% | 实证 | 实测全页 15828px，项目区约 6790px |
| A8 移动端筛选器无滚动提示 | 实证 | 390px 截图 y1600 + mobile.css:144-151 |
| A9 页脚 ASCII 不可读 | 实证 | 1440px y14580 截图 + mobile.css:243 |
| B1–B7 动效与交互提案 | 经验判断（B1 另有源码实证） | 无"改后渲染"证据；B1 依据 desktop.css:513 |
| C1 metrics strip | 经验判断，**且行业共性依据被推翻** | E 节实证：智元、云深处均无数字看板 |
| C2 G1-D 项目时间轴 | 经验判断，有行业间接支持 | E2/E3：里程碑在三家均为独立模块 |
| C3 / C4 / C5 | 经验判断 | 无本项目证据，且 C3/C4 需用户决策 |
| A10 1024px 空黑块 | **实证** | 1024px 截图（空黑块）+ 计算样式 `heroVisual=none / heroGrad=true / scrollInd=flex`；对照 1023（无渐变）与 1025（有螺旋） |
| A11 标题字号反向跳变 | **实证** | 计算样式实测 1023＝71.61px、1024＝48px、1025＝48.06px |
| A12 平板留白骤降 | **实证** | `mainPadL` 实测 768/820/900/1023＝16px、1024/1280＝80px |
| A13 360px 导航溢出 | **实证** | 360px 截图（「关于」被裁）+ `navOverflow sw334/cw326`；375px 对照不溢出 |
| A14 死规则从未生效 | **实证** | 390/375/360 实测 `li:nth-child(1)` 均 MISSING、`li:nth-of-type(1)` 为 `list-item` |
| A15 360–375 Hero 孤字 | **实证** | 375/360 计算 `heroTitleH=126`（3 行）+ 360px 截图确认「师」行 |
| I 节 I1–I15 | **实证** | 见 I 节逐条标注（交互探针 / 无障碍探针 / Network 域统计 / CDP 键鼠事件） |
| H2-7 跨浏览器 | **未验证** | 本环境 Firefox 为 snap 包，受沙箱限制无法使用自定义 profile，且已有用户实例在运行，未强行启动。仅 Chrome 一档为实证 |

**本轮新增的「复核不成立 / 通过项」**（同样按实证口径，避免只留问题清单）：

| 条目 | 等级 | 依据 |
|---|---|---|
| 对比度怀疑（`--color-ghost-grey` on `#232323`）**复核不成立** | 实证 | 全站 217 条文本逐条算对比度：`#DEDEDE` on `#232323` ＝ **11.7:1**（AAA）。确证不达标仅 10 条，集中在 3 个 token 复用点（见 I7 / I8 / I9） |
| 锚点跳转落点 **通过** | 实证 | 点导航「项目」后 `#projects` 顶距视口 96px、导航底 83px、kicker 未被遮挡，scrollspy 高亮同步为「项目」 |
| 无障碍 DOM 基础 **通过** | 实证 | `lang=zh-CN`、单一 `<h1>`、H1→H2→H3 层级完整、20/20 图有 alt、0 个失效锚点、外链全带 `noopener`、0 个 `aria-hidden` 包裹可聚焦元素 |
| 布局稳定性（CLS）**通过** | 实证 | 冷加载 CLS ＝ **0**（虽 20/20 图无 width/height，但均在定比容器内，见 I13） |
| 视频弹窗开关逻辑 **通过** | 实证 | 打开→`is-open` + 注入 `<video src=…>`（960×540）→ ESC / 关闭按钮 / 遮罩点击均可关且清空 `innerHTML` |
| 技术栈筛选逻辑 **通过** | 实证 | 点「ROS 开发」后 7 个分类加 `is-dimmed`、1 个保持可见，逻辑正确（缺可访问状态见 I6） |

- A8/A9 两个移动端/页脚缺陷的修复方式待你确认（遮罩形态、ASCII 是提升可读还是换徽标）

## I. 第三轮：交互 / 无障碍 / 性能审计（H2 的 4–6 项）

> 口径同前：**实证** ＝ 本轮已实测（CDP 键鼠事件 / 无障碍探针 / 逐条对比度计算 / Network 域字节统计 / 截图）；除注明外均为 1440×900 桌面实测。
> 分组：**I-1 键盘可达性**（I1–I5，最严重）· **I-2 语义、状态与对比度**（I6–I9）· **I-3 死代码 / 结构债**（I10–I11）· **I-4 性能与资源**（I12–I15）· **I-5 人工复核项**

### I-1 键盘可达性（最严重）

| # | 现象 | 根因 / 位置 | 处置 |
|---|---|---|---|
| I1 | **视频弹窗完全不可键盘操作**：`.video-cover` 是 `<div>`，`tabIndex=-1`、无 `role`、无 `aria-label`；全站 27 个可聚焦元素中不含它 | `index.html` 572+ 用 `<div class="video-cover">`；`main.js` 只绑 `click` | 改 `<button type="button">`（或补 `role="button" tabindex="0"` + 回车/空格键处理），并补 `aria-label`（用已有的 `.video-cover__label` 文案） |
| I2 ✅ | **弹窗无对话语义、焦点不管理**：打开后 `document.activeElement` 仍是 `BODY`；无焦点陷阱，关闭后不还原焦点 | `main.js:87-97` 动态创建的 modal 无 `role="dialog"` / `aria-modal="true"`，也没有 `focus()` / `inert` 处理 | 补 `role="dialog" aria-modal="true" aria-label="视频播放"`；打开时 `closeBtn.focus()`，关闭时还原到触发封面 |
| I3 ✅ | 弹窗关闭按钮 `×` **实测不可见**（对比度 1.06:1） | `desktop.css:1411` `color: var(--color-muted)` —— 该变量从未声明（见 A16） | 修 A16 即可 |
| I4 🟡 | 点击封面后视频**不自动播放**（实测 `paused=true, currentTime=0`，2.5s 后仍未动） | `<video controls autoplay>` 未静音，被浏览器默认自动播放策略拦截。用 `--autoplay-policy=no-user-gesture-required` 复测可正常播到 2.38s → **代码无错，是策略行为** | 二选一：加 `muted`（但用户看演示视频多半要声音）或接受「需再点一次 play」。另**必补 `playsinline`**，否则 iOS Safari 会强制全屏 |
| I5 ✅ | **全站无自定义焦点样式**：`assets/css` 中 `:focus` / `:focus-visible` 规则 **0 条** | 未写焦点样式 | 加全局 `:focus-visible { outline: 2px solid var(--color-accent); outline-offset: 2px; border-radius: 2px }`。实测现状是 UA 默认 `outline: auto 1px rgb(16,16,16)`，落在 `#242423` / `#000` 深色区几乎不可见 |

### I-2 语义、状态与对比度

| # | 现象 | 根因 / 位置 | 处置 |
|---|---|---|---|
| I6 | 技术栈筛选**无可访问状态**：按钮 `ariaAttrs` 仅 `class` / `data-filter`，无 `aria-pressed`；被 dim 的 7 个分类只改 `opacity`，仍在无障碍树里 | `main.js:151-164` 只切 class | 按钮补 `aria-pressed`（或 `role="tablist"` + `aria-selected`）；被 dim 的分类加 `aria-hidden="true"` |
| I7 ✅ | **浅色区橙色正文对比度 1.95:1**（AA 需 4.5:1） | `.timeline-list__meta` 用 `--color-accent` `#FF9100` 落在 `--color-off-white` `#F1EEE7` 上（实习区「高速路口闸机机器人收递卡项目 · 宇树 G1-D」） | 换深一档橙，实测阶梯：`#CC6E00`＝3.13 · `#B35F00`＝3.98（**仍不达标**）· `#A05200`＝4.90 ✅。或把该行降级为非正文元素。**橙色是品牌色，需你拍板** |
| I8 | 页脚版权行对比度 3.01:1（`rgba(255,255,255,.35)` on `#000`） | 页脚底部弱化文字 | 提高 alpha，实测：`.45`＝4.41（**仍差一点**）· `.55`＝6.27 ✅ |
| I9 | 未选中的筛选按钮对比度 4.30:1（`rgba(255,255,255,.45)` on `#242423`），**临界不达标** | `.tech-filter__btn` 默认色 | 提到 `.5`＝4.96 ✅ 即可 |

> 说明：三项都属「差一点」，但**这是全站扫描 217 条文本后仅有的 10 条不达标项**，其余（含被怀疑的 `--color-ghost-grey` on `#232323`＝11.7:1、`--color-dark-grey` on `#F1EEE7`＝5.96:1）均通过。修掉这 3 个 token 复用点即达全站 AA。

### I-3 死代码 / 结构债

| # | 现象 | 根因 / 位置 | 处置 |
|---|---|---|---|
| I10 | `.nav__toggle`（汉堡按钮）是死元素：HTML 有、`display:none` 全局隐藏、无任何断点显示、`main.js` 无绑定 | `index.html:75` + `desktop.css:115` | 删除（当前移动端走内联横排，不需要汉堡） |
| I11 | **`.hero__status-bar` 已不存在于 HTML**（`index.html` 0 处命中），但 3 个文件仍保留其样式：`style.css:693`、`desktop.css:280`、`mobile.css:52-67` | 元素在某轮改版中被删、样式忘记回收 | 删样式。**它的缺席同时是「移动端 Hero 底部约 198px 空白」的直接原因**：390px 实测内容底 498 / hero 底 760（−padding 64）→ 空白 198px；<1024 时既无状态栏、`.hero__scroll-indicator` 又是 `display:none`，底部无任何锚定元素。处置：删死样式 + 调低移动端 `min-height`，或补一个底部元素 |

### I-4 性能与资源

| # | 现象 | 根因 / 位置 | 处置 |
|---|---|---|---|
| I12 | **首屏即下载 4.48MB 视频**（用户未点任何封面之前）。Network 域实测冷加载总计 **7337KB**：`mp4 4480` / `jpg 1118` / `woff2 1078` / `css 59` / `js 38` | `main.js:169-180` `initVideoCovers()` 对每个 `<video>` 调 `v.load()` 并把 `currentTime` 设到 0.5s → 强行触发媒体数据下载，`preload="metadata"` 被绕过（实测 3 个视频 `readyState=4`、已缓冲 3.4–3.9s） | 改为「进入视口 / 首次 hover 时才截帧」，或直接用静态封面图（需补 3 张，见 F 节） |
| I13 | 20/20 图片**无 `width`/`height`**；首屏 3 张未 `loading="lazy"` | 标签未写尺寸 | 补 `width`/`height` 或 `aspect-ratio`。**实测 CLS ＝ 0**（图片均在定比容器内），故风险等级低，属预防性 |
| I14 | **Geist / Geist Mono / JetBrains Mono / Fira Code 全部未加载** | `index.html:43-45` 只引了 Noto Sans SC；`--font-sans` / `--font-mono` 的首选字体从未获取（Network 域无请求，`document.fonts` 只有 Noto Sans SC 的 40+ 个 subset） | 二选一：①引入 Geist + Geist Mono（自带托管，注意别加阻塞）②若接受现状，把 `--font-mono` 的首选改成实际可得的字体名。**现状 ＝ `--font-mono` 落到系统通用 `monospace`（Linux=DejaVu、macOS=Menlo、Win=Consolas），终端美学跨平台不一致** |
| I15 | 字体样式表是渲染阻塞资源，实测 **1159ms**（当时 Load 事件总 2277ms） | `fonts.googleapis.com` 第三方 CSS | 已有 `preconnect` ✓；可进一步自托管或 `media="print" onload` 异步化。**注意本项为 dev 模式实测（未 gzip），生产数据需在 `npm run build` 产物上复测** |

**性能通过项**：FCP 1472ms / LCP 416ms / **CLS 0** / 无资源放大显示（`imgUpscaled` 为空）—— 见 G 节。

### I-5 人工复核项（需要在真实浏览器里看的）

1. **I4 的播放行为** 🟡 2026-09-15 已补 `playsinline`；**仍待你**用真实 Chrome / Safari 点一次项目视频封面，确认是否真的需要二次点击、iOS 真机是否仍强制全屏
2. **I14 的字体**：请在你自己常用的机器上截图对比等宽字体渲染（本环境是 Linux，落 DejaVu Sans Mono，与设计稿的 Geist Mono 差异明显）
3. ~~**I7 橙色**~~ ✅ **2026-09-14 已定**：保留橙色语义，新增 `--color-accent-deep: #A05200`（4.90:1，实测通过）；如需改判为「降级为非正文」一行可换
4. **A11 / A12 / A15 断点取值**：建议你在 1023 与 1024 各截一张并排看，确认「字号反向跳变 + 留白突变」是否真的刺眼
5. **移动端 Hero 底部空白（I11）**：390px 下向下滑过首屏时的空窗感

## J. 第四轮补充：代码卫生 / SEO 完整性 / 令牌债（2026-09-16 实测）

> 口径：**实证** ＝ 2026-09-16 用 grep 全量计数，每行附可复现判据。**J 节不在前三轮扫描范围内** —— 前三轮看渲染与交互，未查源码卫生，故与 A / I 节不重叠。
> 命令模板：`grep -oh '\-\-[a-zA-Z0-9-]*[[:space:]]*:' assets/css/*.css | sed 's/[[:space:]]*:$//' | sort -u`（声明集合）对比 `grep -oh 'var(--[a-zA-Z0-9-]*' assets/css/*.css index.html | sed 's/var(//' | sort -u`（引用集合），`comm -23` 取差集。

| # | 现象 | 证据 | 处置 |
|---|---|---|---|
| J1 | **暗区背景被内联硬编码，`.section--dark` 的 background 被内联压制**（内联特异性高于 class） | `index.html` 中 `style="…background: #242423"` 共 **5 处**；`#242423` 全站 **8 次**；`.section--dark { background: var(--color-black) }` 位于 `desktop.css:300`（已从媒体查询提到全局档，但仍被内联覆盖） | 删内联 background，让暗区走 class + 令牌。**建议排在 A4 / A6 布局重排之前** —— 否则重排要再动一遍这些内联值 |
| J2 | **色彩系统有 4 个近似黑**，改一次暗色要动 11 处 | `#000000`（`--color-black-deep`，已令牌化）· `#232323`（`--color-black`，**7 次**）· `#242423`（**8 次**）· `#0A0A0A`（**3 次**） | 新增 `--color-cement: #242423`（保留现有观感）或统一到一档；**方案需你拍板** |
| J3 | **80 个令牌里 18 个从未被 `var()` 引用**（JS 也不引用任何令牌，已确认） | 声明 **80** / 引用 **62**；差集：`--space-xs/sm/md/lg/2xl/3xl/4xl/5xl` · `--space-1` · `--space-24` · `--font-cjk` · `--weight-semibold` · `--radius-2` · `--color-glass` · `--color-glass-dark` · `--dotted-bg-dark` · `--ease-in-out` · `--nav-height` | 删死令牌（约 350+ 字节）。**其中 `--nav-height: 60px` 与实测冲突**：`.nav` 桌面写死 `52px`（`desktop.css:18`）、移动 `44px`（`mobile.css:11`）→ **三方不一致**（令牌 60 / 桌面 52 / 移动 44），属真 bug 隐患，删或改都会牵动 nav 高度 |
| J4 | **favicon 完全缺失** | `grep 'rel="icon"\|favicon\|apple-touch' index.html` = **MISSING**；无 `public/`，根目录无 `favicon.ico` | 加 favicon（页头已有内联品牌 SVG，可复用）。用户可见：标签页空白 + 控制台 404。**一行级** |
| J5 | **robots.txt / sitemap.xml 缺失** | 根目录无这两个文件，也无 `public/` | SEO 的 meta / OG / twitter:card / canonical / JSON-LD **已齐**，独缺这两项；静态站各一个文件即可 |
| J6 | **导航高亮无 `aria-current`** —— 当前区块只靠 `.nav--active` class，读屏读不出「你在哪」 | `aria-current` 在 `index.html` 与 3 个 js 中命中 **0** | 在 `main.js` 的 scrollspy 里补 `aria-current="location"`；与 I6（筛选 `aria-pressed`）同族，建议同批 |
| J7 | **死样式规模远大于 I10 / I11 所记**（review 只点了 2 处） | `index.html` + 3 个 js **全 0 命中**的类：`ide-window*`(9) · `skills-list*`(5) · `radial-text__*`(5) · `hero__status-bar` + `status-bar__*`(4) · `section__desc` · `link-dashed` · `chip-card` · `dotted-overlay` · `grid-12` · `section--black`（合计约 28 个类） | 一次清干净。**`sr-only` 与 `char-matrix*` 建议保留**（前者是无障碍工具类，后者已否决但按决策留作备用） |
| J8 | **冗余内联样式**：全文 **29 处 `style="`** | 含 **3 处 `text-decoration:none`**（全局 `a { text-decoration: none }` 已有，纯冗余）+ 5 处重复 `margin-bottom: var(--space-4)` | 冗余的删、重复的提成工具类；属 J1 的同一类债 |

**顺带复核通过（勿当缺陷修）**：`prefers-reduced-motion` **已全局覆盖**（`style.css` 2 处，任何新增 CSS 动效自动降级）· SEO meta / OG / JSON-LD 已齐 · `mono-label` 在用（18 处）· `text-watermark` 在用 · **JS 不引用任何设计令牌**（故 J3 的差集判定成立）。

**J 节建议执行顺序**：J4 + J6（各一行级、用户与无障碍可见）→ J1 + J2（令牌化，先于布局重排）→ J3 + J7 + J8 + I10（纯清理，零视觉风险，可一次提交）→ J5（两个静态文件，顺手）。

> ⚠️ **待实测**：`.contact-item-big` 是 flex 且**无 `flex-wrap`**，移动端 `__value` 字号为 `--text-h3`、右侧两枚 `.contact-chip`（各 `min-width: 5.5em`）+ 40px 图标 —— 360px 下（内容宽约 320px）微信行是最挤的一行，**疑横向溢出**。本条为 2026-09-15 新增微信行引入的风险，因 dev server 已停、探针目录被系统清理，**本轮未实测**，需起服务补测后定级。

## H. 本轮完成 / 未完成清单（交接下一会话）

### H1 已完成（可直接依赖）

| 范围 | 覆盖 | 证据 |
|---|---|---|
| 桌面 1440×900 | 逐屏 **20/20 全覆盖** | 首轮核对 17 张 + 补看 15/17/18 |
| 移动 390×844 | **全覆盖**（第三轮补齐）| 定点 y0 Hero / y9000 项目 / y10250 实习 / y11000 荣誉 / y12300 生活 / y13400 页脚 + 整页 2 段 |
| 断点实测 | **11 档**：360 / 375 / 390 / 768 / 820 / 900 / 1023 / 1024 / 1025 / 1280 / 1440 | CDP 计算样式 + 定点截图 |
| 交互态 | hover / 锚点跳转落点 / 筛选点击 / 弹窗开关与播放 / Tab 顺序（16 步） | CDP `Input.dispatchKeyEvent` + 脚本事件 |
| 无障碍 | 键盘可达性 · 焦点样式 · 对比度（217 条文本逐条计算）· 语义与 aria · 图片 alt | 无障碍探针 + 对比度探针 |
| 性能 | 传输字节（Network 域）· FCP/LCP/CLS · 字体实际加载 · 图片懒加载 | Network + Resource Timing + PerformanceObserver |
| 参考站正文抓取 | **4 个** | 宇树 / 智元 / 云深处 / Awwwards SOTD《Robot》 |
| 源码通读 | index.html 981 + style.css 881 + desktop.css 1561 + mobile.css 251 + main.js 188 + animations.js 56 | 全文读过 |
| 全页高度实测 | 桌面 1440＝15769px / 1280＝15092px / 移动 390＝14227px | CDP 实测（第三轮复测；A7 记录的 1440＝15828px 是第一轮数值，两者差 59px，属后续布局微调所致） |

### H2 未完成（下一会话按序执行）

**1. 跨浏览器（H2-7，唯一未完成项）** —— 本环境 Firefox 是 snap 包（`/snap/firefox/…`），受沙箱限制无法使用自定义 profile（报 `Could not find profile folder`），且已有用户实例在运行，未强行启动。**需在你自己机器上补 Safari / Firefox 两档**。重点看：I14 的等宽字体、A11 的断点字号、B 节动效提案（若有）、`svh` 单位（hero `min-height: 100svh` / `90svh` 在旧 Safari 上不支持，需确认降级）

**2. 生产构建复测** —— 本轮全部数据来自 `npm run dev`（无 gzip、无打包）。I12/I15 的体积与耗时必须在 `npm run build` + `npm run preview` 产物上复测才有交付意义

**3. A–I / J 节的修复落地** —— 按 D 节顺序。截至 2026-09-16：**已落地** A2 / A10 / A13 / A14 / A16 / B1 / B2 / I2 / I3 / I5 / I7；**部分** I4（`playsinline` 已补，自动播放策略未决）；**未动手** A1 / A3 / A4 / A6 / A7 / A8 / A9 / A11 / A12 / A15 / B3 / B4 / B5 / C1–C5 / I1 / I6 / I8 / I9 / I10 / I11 / I12 / I13 / I14 / I15 + **J1–J8**

**4. 已否决项（勿重走）** —— 通栏黑带分隔条 · 全站统一暗色 · 竖版视频适配 · char-matrix 接入 · **B6 锚点落点描边（2026-09-15）** · **B7 Hero SCROLL 刻度（2026-09-15）**

### H3 复现环境（下一会话照做）
- **node 不在默认 PATH**：先 `export PATH="$HOME/.nvm/versions/node/v22.23.1/bin:$PATH"`（node v22.23.1 / npm 10.9.8）
- 起服务：`npm run dev`（5173 常被占用，会自动跳 5174，读日志里的实际端口）；**动手前先 `curl` 一次确认端口**——本轮若端口猜错，所有探针都会静默测到别的服务
- 截图 / 测量探针：本轮已重建并留在 **`/tmp/probe/`**（在仓库外，不会污染 git）：`cdp.mjs`（截屏 / 定点 / eval）、`breakpoint.js`（断点指标）、`roles.js`、`interact.mjs`（交互 + Tab + 无障碍）、`contrast.js`（逐条对比度）、`perf.js` / `perf2.mjs`（体积 / LCP / CLS）。用法：`node cdp.mjs eval --w 1440 --h 900 --port 9901 --file xxx.js`（**端口必须每档唯一，且别写成 `9$w` 这种会超过 65535 的拼法**）
- 参考站**无需重抓**；复抓方式：`web_fetch` + 指定 `fetchInfo`（比 anysearch extract 省 token）
- 探针踩坑：`--virtual-time-budget` 会跳过平滑滚动与 rAF；hero `min-height:100svh` 会被超大 `--window-size` 撑坏 → 用 CDP `captureBeyondViewport` 分段截（每段 ≤8000px）；`#projects` 这类锚点跳转要用 `Runtime.evaluate` 触发放平滑滚动后再读 `scrollY`
- **对比度测量的时序坑**：hero 的 kicker/title/CTA 用的是 `animation: fadeUp … forwards`（起始 `opacity: 0`、延迟 0.1–0.35s）。**页面加载后必须等 ≥2.5s 再读 computed style**，否则会把「动画尚未开始」读成 `opacity: 0`，进而误判成「内容不可见 / 对比度 1:1」。本轮第一版对比度脚本就踩了这个坑（`.cta-split__left`、`.cta-split__right` 被误报为 1:1），第二版加了等待与 `<2.5s 剔除` 才得到可信结果。注意 `.animate-on-scroll`（`desktop.css:513`）本身已是 `opacity: 1; transform: none`，与 A2 一致，不是坑源

> H2 的 1–3 项（渲染 / 断点）已按建议扩进 **A 节（A10–A16）**；4–6 项（交互 / 无障碍 / 性能）按建议独立为 **I 节**，未混进 A–C。H2-7 跨浏览器未完成，见上。
